# 待办状态

更新时间：2026-05-21

状态说明：
- 未开始：尚未处理。
- 进行中：已有对话或开发分支正在处理。
- 待验证：代码已改完，需要测试或人工确认。
- 待配置 / 待部署：代码就绪，需在部署/运维侧填配置。
- 部分完成：主体已做，仍有遗留依赖未补齐。
- 暂缓：暂不处理，但仍保留记录。

> 本文档仅保留**未完成项**。

---

## 按阶段排序（共 20 项）

> 按"什么时候做"分组，方便排迭代。详细描述见下面各小节。

### 🚨 部署阻塞（3 项，不做不能上线）

- 凭据轮换（见 §高优先级）
- `SESSION_SECURE_COOKIE=true`（见 §2026-05-16 商业可用性审查 M3）
- EPUSDT 后台 CORS 生产收紧（见 §安全与平台）

### ⚙️ 部署期配置（2 项，上线时填密钥/参数）

- reCAPTCHA 密钥申请 + 填值 + 端到端验证三端点（见 §安全与平台）
- `EPUSDT_QUERY_ORDER_PATH` 配置（启用漏回调主动对账，见 §安全与平台 "支付对账任务"）

### 🛡️ 上线后加固（9 项，不阻塞，按优先级排）

- **H1–H4**：CDNfly 重试 / `ensureOutboundEnabled` 业务错误 / 管理审计日志 / EpusdtService 异常脱敏
- **M1–M2**：管理员强制 2FA / 订单过期时间可配置
- 前端路由 `beforeEach` 守卫（见 §2026-05-16 审计新增）
- 邮件外部告警通道（Slack / 邮件，见 §安全与平台）
- 删除 `APP_PREVIOUS_KEYS` 残留引用（见 §安全与平台）

### 📦 产品功能补全（6 项，独立业务线）

- 用户端控制台流程接通 CDNfly（2 条"进行中"，见 §产品清理）
- 品牌资产替换：控制台 Logo / Favicon / 邮件模板 Logo（见 §产品清理）
- `/verify-email` 已验证用户跳转优化（见 §产品清理）
- 部署说明从页面提示搬到文档（见 §产品清理）

---

## 高优先级

| 状态 | 待办 | 备注 |
| --- | --- | --- |
| 未开始 | 审查报告脱敏，并轮换已泄露的真实凭据 | 优先处理，详见 HANDOVER §8.8 |

## 安全与平台

| 状态 | 待办 | 备注 |
| --- | --- | --- |
| 待配置 | reCAPTCHA v2 Checkbox 防刷（注册/登录/忘记密码） | 代码已就绪，覆盖 `POST /api/auth/register`、`POST /api/auth/login`、`POST /forgot-password` 三个端点；`RecaptchaService::validate()` 在 secret 为空时静默跳过。`.env` 中 `RECAPTCHA_SITE_KEY` / `RECAPTCHA_SECRET_KEY` 均为空，需申请 Google reCAPTCHA v2 密钥填入。**密钥填好后须端到端验证三个端点**（原 M4 已合并至此） |
| 未开始 | 邮件发送层频控告警——增加外部通知通道（Slack/邮件） | ThrottleOutboundMail 已有频控计数 + 日志预警，缺外部告警 |
| 部分完成 | 支付对账任务：处理漏回调、过期订单、已支付未开通重试 | `payments:reconcile` 已每 5 分钟调度，但第 1 步"EPUSDT 主动对账"依赖 `EPUSDT_QUERY_ORDER_PATH`，该值在 `.env` 中为空，导致回调丢失时无法自动恢复已支付订单 |
| 待部署 | EPUSDT 后台 CORS 生产加固：将允许来源收紧为生产管理域名白名单 | `cors.php` 已改为读取 `CORS_ALLOWED_ORIGINS` 环境变量，部署时配置即可 |
| 未开始 | 确认兼容窗口结束后删除 `APP_PREVIOUS_KEYS` | 当前 `.env` 值已为空，但 `.env.example:9`、`config/app.php:103-104` 仍引用该变量 |

## 产品清理

| 状态 | 待办 | 备注 |
| --- | --- | --- |
| 进行中 | 设计与开发登录后的控制台（Console/Dashboard） | 管理端核心页面已推进到真实模块；用户端开通套餐、添加网站、站点配置等流程仍待补齐 |
| 进行中 | Console 前后端接通 CDNfly 实际接口 | 管理端所有模块已挂载；本地开发默认禁用出站；用户端开通套餐、网站配置保存等仍待接 |
| 未开始 | 上线前替换控制台品牌图标/Logo | 当前用临时 CDN 节点图标 |
| 未开始 | 上线前统一邮件模板 Logo 和品牌露出 | 验证邮件、找回密码等通知模板 |
| 未开始 | 正式上线时，`/verify-email` 对已登录且已验证用户直接跳转 `/dashboard` | |
| 未开始 | 把部署说明从页面提示彻底移到文档 | |

## 2026-05-16 审计新增

| 状态 | 优先级 | 待办 | 涉及文件 | 备注 |
| --- | --- | --- | --- | --- |
| 未开始 | P2 | 前端路由添加 `beforeEach` 导航守卫，检查登录状态 | frontend/src/router/index.js | 当前无任何前端访问控制 |

> EPUSDT_QUERY_ORDER_PATH 已合并至 §安全与平台 "支付对账任务"，不在此重复。

## 2026-05-16 商业可用性审查

优先级：C = 阻断上线，H = 高风险（影响支付/业务连续性），M = 中风险（影响体验/运维）。

> C 级阻断项（C1–C4）已全部完成，明细见备份。

| 状态 | 优先级 | 待办 | 涉及文件 | 备注 |
| --- | --- | --- | --- | --- |
| 未开始 | H1 | CDNfly HTTP 调用增加重试机制 | CdnflyApiService.php | 所有 CDNfly 请求无 retry/backoff，上游短暂不可用时管理端全部返回 502 |
| 未开始 | H2 | `ensureOutboundEnabled` 改为返回业务错误而非抛 RuntimeException | CdnflyApiService.php:114-124 | 出站禁用时返回 500，应为明确的 503 + 业务错误 JSON |
| 未开始 | H3 | 管理操作增加结构化审计日志 | AdminController.php 所有写方法 | 管理员增删改用户/套餐/节点无审计记录，出事后无法追溯操作人/时间/内容 |
| 未开始 | H4 | `parseCreateTransactionResponse` 异常消息脱敏 | EpusdtService.php:150,154,159,164 | 抛出异常时包含 EPUSDT 原始响应体 JSON，可能含敏感数据进入日志 |
| 未开始 | M1 | 管理员强制 2FA | config/fortify.php, AdminController | 当前 2FA 可选，管理员账号被接管后无第二因素保护 |
| 未开始 | M2 | 订单过期时间可配置化 | EpusdtCheckoutService.php:73 | `expire_at` 硬编码 30 分钟，部分支付场景可能需要更长时间 |
| 待部署 | M3 | 生产环境 `SESSION_SECURE_COOKIE=true` | .env:55 | 当前为 false，部署时必须改为 true 强制 HTTPS Cookie |

> M4（reCAPTCHA 端到端验证）已合并至 §安全与平台 "reCAPTCHA v2 Checkbox 防刷"，不在此重复。

---

## 已就绪

以下方面经审查符合商业上线标准（保留作为状态参考，**非待办**）：

- 限流覆盖：所有认证端点 + 全部管理写操作均有 throttle
- 支付幂等：EpusdtController::notify 签名校验 + `lockForUpdate` + trade_id 去重
- CDNfly 凭证安全：强制 HTTPS、日志脱敏（SENSITIVE_KEYS）、独立加密密钥（CDNFLY_ENCRYPTION_KEY）
- 管理端 CRUD：11 模块就绪，Switch + ConfirmDeleteDialog + toast 全覆盖
- 订单归属隔离：OrderController 按 user_id 过滤，管理员除外
- redirect 安全：resolvePostAuthRedirect() origin 白名单校验
- CSRF：http.js 419 自动复位 + 重试
- 管理员自我保护：不能删除自己、至少保留一个管理员
- 前端 UX：window.confirm 清零、successMessage 全部 toast
- 邮箱验证强制：所有 `/cdn/*` `/admin/*` `/orders` `/payments/epusdt/create` 等业务接口强制 `verified` 中间件；管理员后台创建用户强制走验证邮件流程
- Token 过期：Sanctum `SANCTUM_EXPIRATION=43200`（30 天）安全上限
- 登录链路：Fortify web controller 包装已收成 API 实现，2FA / 密码重置路由已统一 API 风格

