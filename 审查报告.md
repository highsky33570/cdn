# 代码安全与质量审查报告

**审查人：** Claude Sonnet 4.6 (Anthropic)
**时间戳：** 2026-04-24T18:00:00+08:00
**项目后端：** tycdn-backend（Laravel 框架）
**项目前端：** frontend/src（Vue 3 + Vite）
**审查范围：** 路由、控制器、中间件、模型、服务层、前端 API 层及配置文件

---

## 目录

1. [严重问题（🔴）](#严重问题)
2. [中等问题（🟠）](#中等问题)
3. [小问题（🟡）](#小问题)
4. [优点（✅）](#优点)
5. [优先修复顺序](#优先修复顺序)

---

## 严重问题

### 🔴 S-01：订单创建接口无需认证，且 `user_id` 由客户端自由传入

**文件：** `routes/api.php`（第 72–74 行）、`app/Http/Controllers/Api/OrderController.php`（第 13–44 行）

三条订单路由均位于任何认证中间件之外，任何匿名请求者均可：

1. 以任意 `user_id`（包括他人 ID）创建订单（`OrderController::store` 第 19 行 `'user_id' => ['nullable', 'integer']`），导致订单伪造、账户归属篡改。
2. 通过 `show` 枚举所有订单（`order_no` 虽为随机但格式可预测：`TY` + 时间戳 + 4 位随机数）。
3. 对任意已付款订单触发 `provision`，导致未授权开通服务。

**风险：** 攻击者可创建归属于任何用户的订单，并在付款后为该用户非法开通服务；也可枚举他人订单信息（金额、付款地址等）。

---

### 🔴 S-02：支付通知接口（Tron）无签名校验

**文件：** `app/Http/Controllers/Api/PaymentController.php`（第 14–96 行）

`POST /api/payments/tron/notify` 路由对传入的支付通知数据仅做字段格式校验，**没有任何 HMAC/签名验证**。任何人只要知道接口地址，均可构造虚假支付通知并传入真实的 `to_address` 和任意 `txid`，导致订单被标记为已付款、服务被非法开通。

EpusdtController 实现了签名验证（第 118 行），但 Tron 通知控制器完全没有类似机制。

**风险：** 攻击者可免费开通 CDN 服务，造成直接经济损失。

---

### 🔴 S-03：EpusdtController 创建支付订单无需认证，`user_id` 由客户端传入

**文件：** `routes/api.php`（第 76 行）、`app/Http/Controllers/Api/EpusdtController.php`（第 18–108 行）

`POST /api/payments/epusdt/create` 路由无认证保护，且 `user_id` 字段为可选的客户端传入值（第 24 行），与 S-01 属同类问题。任意匿名用户均可以他人 `user_id` 发起支付请求，在支付成功后将服务开通至目标用户账户，或借此对目标用户的账户状态造成干扰。

---

### 🔴 S-04：AdminController 管理代理接口路径过滤不完整，存在敏感端点暴露风险

**文件：** `app/Http/Controllers/Api/AdminController.php`（第 14–17 行、第 240–260 行）

管理端代理黑名单只屏蔽了两个前缀（`/v1/api-key`、`/v1/login`）。`/v1/users` 仅在请求包含 `token=1` 参数时才被屏蔽，其他对 `/v1/users` 的 GET/POST/PUT/DELETE 均被放行。这意味着已登录管理员可通过代理接口直接操作 CDNfly 上游所有用户数据。此外，CDNfly 上游可能存在其他敏感路径全部未被屏蔽。

**与 CdnProxyController 对比：** 用户代理定义了更严格的 `BLOCKED_PREFIXES`（第 12–17 行），包含 `/v1/users`、`/v1/nodes`、`/v1/configs`、`/v1/api-key`，而管理代理却明显更宽松，形成不一致的安全策略。

---

### 🔴 S-05：CdnProxyController 代理接口将用户原始请求不经过滤直接转发

**文件：** `app/Http/Controllers/Api/CdnProxyController.php`（第 48–53 行）

用户的请求体（`$request->all()`）被完整透传至 CDNfly 上游，未做任何字段白名单过滤。若 CDNfly API 内部存在批量赋值或特权字段（如 `user_id`、`type`、`status` 等），用户可通过构造特殊请求体篡改这些字段。`AdminController` 的代理实现（第 221–222 行）存在同样问题。

---

## 中等问题

### 🟠 M-01：密码最小长度过短，强度要求偏弱

**文件：** `app/Http/Controllers/Api/AuthController.php`（第 32 行）

最小长度仅 6 位，仅要求同时包含字母和数字，没有要求包含特殊字符或实施密码黑名单（如常见弱密码 `abc123`、`pass12` 均符合）。对于涉及金融支付的平台，此强度不符合行业安全基线。

**建议：** 最小长度提高至 8 位，引入 `Password::defaults()` 规则，增加大小写混合或特殊字符要求。

---

### 🟠 M-02：订单号生成存在碰撞风险

**文件：** `app/Http/Controllers/Api/OrderController.php`（第 27 行）、`app/Http/Controllers/Api/EpusdtController.php`（第 37 行）

同一秒内并发创建订单时（1 秒内 `YmdHis` 部分相同），4 位随机数仅有 9000 种可能，高并发下极易碰撞，导致 `order_no` 重复并触发数据库唯一约束异常，进而引起订单创建失败。

**建议：** 改用 `Str::uuid()`、`Str::ulid()` 或加入微秒、进程 ID 等更多熵源。

---

### 🟠 M-03：Sanctum Token 永不过期

**文件：** `config/sanctum.php`（第 50 行）

`'expiration' => null`，Token 没有配置过期时间，一旦会话 Cookie 或 Token 被泄露，攻击者可永久持有有效凭证。

**建议：** 根据业务场景设置合理的 `expiration`（如 `60 * 24 * 7` 表示 7 天）。

---

### 🟠 M-04：CORS 配置中的通配符模式过于宽泛

**文件：** `config/cors.php`（第 19–21 行）

两个正则允许 `http://localhost:任意端口` 和 `http://127.0.0.1:任意端口` 发起跨域请求，且同时允许 `http://`（非加密）。在生产环境中，这意味着本机任何端口上的 Web 应用均可通过 CORS 访问 API。

**建议：** 生产部署时通过 `CORS_ALLOWED_ORIGINS` 明确设置生产域名（仅 `https://`），并在生产环境删除或收紧 `allowed_origins_patterns`。

---

### 🟠 M-05：前端硬编码了本地开发地址作为 API 地址回退

**文件：** `frontend/src/config/runtime.js`（第 11 行）

若生产构建时未设置 `VITE_API_BASE_URL` 环境变量，应用将静默回退到本地开发地址，导致生产环境所有 API 请求失败且无明显报错。此外，该地址为明文 `http://`，不适合生产环境。

**建议：** 生产构建时强制要求 `VITE_API_BASE_URL` 存在，在缺失时抛出构建错误而非静默回退；同时确保生产地址使用 `https://`。

---

### 🟠 M-06：`PaymentController::notify` 捕获异常后将原始错误信息返回给客户端

**文件：** `app/Http/Controllers/Api/PaymentController.php`（第 89–94 行）

`'error' => $e->getMessage()` 可能包含 SQL 语句、数据库结构、文件路径等内部实现细节，帮助攻击者收集系统信息（信息泄露）。

**建议：** 移除 `'error'` 字段，仅对内部记录异常日志，向客户端返回通用错误描述。

---

### 🟠 M-07：`EpusdtService::sign` 使用 MD5 作为签名算法

**文件：** `app/Services/EpusdtService.php`（第 34 行）

MD5 已被密码学界弃用（碰撞攻击已实际可行），用于支付签名验证存在安全风险。虽然此处是对接第三方 Epusdt 网关的现有协议，但若该网关支持更强的签名算法（如 HMAC-SHA256），应优先使用。

**建议：** 确认 Epusdt 网关是否支持 HMAC-SHA256 等更强签名方案；如不支持，在代码注释中明确标注此为第三方协议限制。

---

### 🟠 M-08：`AdminController::createPackage` 和 `updatePackage` 无输入验证

**文件：** `app/Http/Controllers/Api/AdminController.php`（第 145–153 行、第 157–165 行）

`$request->all()` 未经任何验证直接传递至 CDNfly API，缺乏本地验证意味着一旦 CDNfly 侧存在漏洞，恶意参数可直接到达；同时调试和错误追踪也更困难。

---

## 小问题

### 🟡 Q-01：`verifyEmail` 使用 SHA-1 计算邮箱哈希

**文件：** `app/Http/Controllers/Api/AuthController.php`（第 93 行）

Laravel 官方邮箱验证默认使用 `sha1`，此处与框架约定一致，使用了 `hash_equals` 防止时序攻击，当前风险较低，但建议在框架升级时跟进更强算法。

---

### 🟡 Q-02：前端密码确认字段直接使用原密码

**文件：** `frontend/src/api/auth.js`（第 30 行）

`password_confirmation` 直接复制 `password` 字段值，而非来自用户独立输入的确认密码字段，前端的"确认密码"校验形同虚设，无法捕捉用户输入错误。

---

### 🟡 Q-03：前端 `fetchForgotPasswordCaptcha` 调用的接口不存在于路由定义中

**文件：** `frontend/src/api/auth.js`（第 45–49 行）、`routes/api.php`

后端路由定义中不存在 `/api/auth/forgot-password/captcha` 端点，此函数调用将返回 404，属于死代码或待实现功能。

---

### 🟡 Q-04：`OrderController::show` 不验证调用者是否为订单所有者

**文件：** `app/Http/Controllers/Api/OrderController.php`（第 46–53 行）

即使后续为该路由添加认证，也需要增加所有权校验（`$order->user_id === Auth::id()`），否则任何登录用户可查询他人订单详情。

---

### 🟡 Q-05：路由文件中无认证的订单/支付路由缺乏结构化分组

**文件：** `routes/api.php`（第 71–77 行）

无认证路由与有认证路由混在同一文件中，缺乏结构化分组（如独立的 `Route::group`），容易在后续维护中误将敏感接口漏加中间件。

---

### 🟡 Q-06：前端路由无导航守卫，受保护页面无访问控制

**文件：** `frontend/src/router/index.js`（完整文件）

路由定义中没有任何 `beforeEach` 导航守卫来检查登录状态，未登录用户可在前端直接访问任何路由路径。虽然后端 API 仍会进行认证，但用户体验存在缺陷，且若后续添加纯前端保护的页面将产生安全盲区。

---

### 🟡 Q-07：`EpusdtController::notify` 中的 `from_address` 字段写入空字符串

**文件：** `app/Http/Controllers/Api/EpusdtController.php`（第 183 行）

`from_address` 被硬编码为空字符串，降低了支付记录的可追溯性和事后核查能力。

---

### 🟡 Q-08：`CdnflyProvisionService` 中 `http()` 方法当 token 为空时静默跳过认证

**文件：** `app/Services/CdnflyProvisionService.php`（第 231–243 行）

当 `CDNFLY_AUTH_TOKEN` 未配置时，HTTP 请求将不携带任何认证头，可能导致向上游发送未认证的请求。应在调用时对此进行明确的配置检查并抛出异常。

---

### 🟡 Q-09：`.env.example` 中 `APP_DEBUG=true`

**文件：** `tycdn-backend/.env.example`（第 5 行）

示例环境文件将 `APP_DEBUG` 设为 `true`，新开发者或自动化部署脚本直接复制此文件时，可能在生产环境开启调试模式，导致详细错误堆栈向客户端暴露。

**建议：** 将示例值改为 `APP_DEBUG=false`，并在注释中说明仅开发环境可设为 `true`。

---

### 🟡 Q-10：`csrfReady` 为模块级全局变量，存在会话失效后状态污染风险

**文件：** `frontend/src/api/http.js`（第 3 行）

`csrfReady` 一旦置为 `true` 便不会再次请求 CSRF cookie。若 CSRF token 因会话过期而失效，后续请求将使用过期 token 导致 419 错误。应在收到 419 响应时重置 `csrfReady` 并重试。

---

## 优点

### ✅ A-01：CDNfly API 密钥和 Secret 使用 Laravel 加密字段存储

**文件：** `app/Models/User.php`（第 33–34 行）

使用 Laravel 的透明加密 Cast，即使数据库被泄露，API 密钥也不会以明文暴露。

---

### ✅ A-02：User 模型的 Hidden 属性配置完整

**文件：** `app/Models/User.php`（第 15 行）

`password`、`two_factor_secret`、`two_factor_recovery_codes`、`remember_token`、`cdnfly_api_key`、`cdnfly_api_secret` 均在 `Hidden` 中声明，有效防止这些字段在序列化时意外暴露。

---

### ✅ A-03：Epusdt 回调实现了完善的签名验证与幂等保护

**文件：** `app/Http/Controllers/Api/EpusdtController.php`（第 110–131 行）、`app/Services/EpusdtService.php`（第 63–71 行）

- 使用 `hash_equals` 进行时序安全的签名比较。
- 使用数据库事务 + `lockForUpdate` 防止并发重复处理。
- 检查 `txid` 唯一性防止重复入账。

---

### ✅ A-04：CdnflyApiService 实现了日志脱敏

**文件：** `app/Services/CdnflyApiService.php`（第 14–29 行、第 434–468 行）

定义了 `SENSITIVE_KEYS` 常量和 `redactSensitivePayload` 方法，在将 CDNfly 响应写入日志前自动屏蔽敏感键值，防止 API Key / Secret 泄露到日志系统。

---

### ✅ A-05：注册接口采用数据库事务包裹用户创建和验证邮件发送

**文件：** `app/Http/Controllers/Api/AuthController.php`（第 40–51 行）

使用 `DB::transaction` 确保用户创建和邮件发送通知在同一原子操作内，避免数据不一致。

---

### ✅ A-06：认证相关接口配置了速率限制

**文件：** `routes/api.php`（第 22–34 行）

注册、登录、邮件重发、API Key 同步均配置了 `throttle` 中间件，有效缓解暴力破解和滥用攻击。

---

### ✅ A-07：前端 HTTP 工具正确实现了 CSRF Token 注入与 Credentials 携带

**文件：** `frontend/src/api/http.js`（第 85–106 行）

所有请求前确保 CSRF Cookie 已获取，正确设置 `credentials: 'include'`，并在请求头中注入 `X-XSRF-TOKEN`，符合 Laravel Sanctum SPA 认证规范。

---

### ✅ A-08：EnsureAdmin 和 EnsureCdnflyApiKey 中间件职责单一、逻辑清晰

**文件：** `app/Http/Middleware/EnsureAdmin.php`、`app/Http/Middleware/EnsureCdnflyApiKey.php`

中间件实现简洁，职责明确，返回统一的 JSON 格式错误响应，与 API 风格一致。

---

### ✅ A-09：`.gitignore` 配置完善，正确排除了 `.env` 及各类 IDE 配置

**文件：** `tycdn-backend/.gitignore`

`.env`、`.env.backup`、`.env.production` 以及主流 IDE 配置目录均被忽略，降低了敏感信息意外提交到版本库的风险。

---

## 优先修复顺序

以下按照**风险影响 × 利用难度**综合排序，建议按此顺序依次处理：

| 优先级 | 编号 | 问题描述 | 修复要点 |
|--------|------|----------|----------|
| **P1** | 🔴 S-02 | Tron 支付通知无签名校验 | 为 `PaymentController::notify` 实现 HMAC 签名验证，与 Epusdt 保持一致 |
| **P2** | 🔴 S-01 | 订单接口无认证且 `user_id` 由客户端传入 | 将订单路由纳入 `auth:sanctum` 中间件，`user_id` 从 `Auth::id()` 获取 |
| **P3** | 🔴 S-03 | Epusdt 支付创建无认证且 `user_id` 由客户端传入 | 同 S-01，为该路由添加 `auth:sanctum`，`user_id` 从服务端获取 |
| **P4** | 🔴 S-04 | 管理代理路径过滤不完整 | 补充 `/v1/users`（无条件）等敏感路径到黑名单，或改用白名单模式 |
| **P5** | 🔴 S-05 | 代理接口透传用户原始请求体 | 对代理接口实现字段白名单过滤，拒绝传递特权字段 |
| **P6** | 🟠 M-06 | 支付异常信息泄露 | 移除 `PaymentController` 中 `'error' => $e->getMessage()` 的返回 |
| **P7** | 🟠 M-01 | 密码强度不足 | 提高最小长度至 8 位，引入 `Password::defaults()` |
| **P8** | 🟠 M-03 | Sanctum Token 永不过期 | 配置合理的 `expiration` 值 |
| **P9** | 🟠 M-05 | 前端生产地址硬编码回退 | 强制要求构建时提供 `VITE_API_BASE_URL` |
| **P10** | 🟠 M-02 | 订单号生成碰撞风险 | 改用 UUID/ULID 或增加更多熵源 |
| **P11** | 🟡 Q-09 | `.env.example` 中 `APP_DEBUG=true` | 改为 `APP_DEBUG=false` |
| **P12** | 🟡 Q-02 | 前端密码确认字段未独立 | 注册表单传入真正的用户二次输入值 |
| **P13** | 🟡 Q-08 | Provision Service 无 token 时静默无认证 | 在 `http()` 方法中对空 token 抛出配置异常 |
| **P14** | 🟡 Q-10 | CSRF Ready 状态不随会话失效重置 | 在 419 响应时重置 `csrfReady` 并重新获取 |
| **P15** | 其余 Q | 其余小问题 | 按开发资源逐步处理 |

---

*报告完毕。以上所有分析均基于代码静态审查，不包含动态运行时测试。建议结合渗透测试和自动化安全扫描（如 OWASP ZAP）进行综合评估。*
