# Vue 3 + Vite

Application styles live in `src/styles/`, imported through `global.css`; Vue files must not contain style blocks. Run `npm run check:styles` to audit both the frontend and console. The audit also runs before production builds. See [the global CSS guide](../docs/global-css.md) for stylesheet ownership and namespace conventions.

This template should help get you started developing with Vue 3 in Vite. The template uses Vue 3 `<script setup>` SFCs, check out the [script setup docs](https://v3.vuejs.org/api/sfc-script-setup.html#sfc-script-setup) to learn more.

Learn more about IDE Support for Vue in the [Vue Docs Scaling up Guide](https://vuejs.org/guide/scaling-up/tooling.html#ide-support).
