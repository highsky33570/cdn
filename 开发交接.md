# TyCDN 项目交接文档

**目录**

1. [项目是什么](#1-项目是什么)
2. [技术栈与关键术语](#2-技术栈与关键术语)
3. [仓库目录地图](#3-仓库目录地图)
4. [本地启动](#4-本地启动)
5. [业务流程与核心代码路径](#5-业务流程与核心代码路径)
6. [项目当前状态](#6-项目当前状态)
7. [数据库](#7-数据库)
8. [生产部署](#8-生产部署)
9. [文档与代码索引](#9-文档与代码索引)

---

## TL;DR — 接手第一周路线图

| 阶段 | 做什么 | 配合阅读 |
|---|---|---|
| **D1** | 跑起项目（§4），登录管理后台把 11 个菜单都点一遍 | §2 末尾的 Laravel / Vue / Inertia 速览三个文档 |
| **D2** | 摸清架构：通读本文档 §3 + §5 + §6.1 | [`待办.md`](待办.md) |
| **D3** | 顺着 [`routes/api.php`](tycdn-backend/routes/api.php) 每个路由点进对应 Controller。如果有真实 CDNfly 测试环境，按 §8.9 启动 EPUSDT 流程试一次 | §5、§7 |
| **D4–5** | 挑两个 H 级加固落地（推荐 H1 CDNfly 重试 + H3 审计日志），熟悉代码组织 | §6.2 |
| **周末** | 备好部署期配置（reCAPTCHA 申请、EPUSDT 查单路径、生产域名清单） | §6.2 部署期必做 |
| **上线前** | 严格按 [`上线前运维.md`](上线前运维.md) 逐项勾掉。**凭据轮换是最高优先级，没做完不要上线** | §8 全章 |

---

## 1. 项目总览（技术方案）

### 1.1 业务定位

**TyCDN = CDN 转售门户**。上游 CDN 能力来自第三方 **CDNfly**，本项目做中间商：

> 用户在门户买套餐 → 系统收 USDT → 自动调 CDNfly 给用户开通 CDN 服务 → 管理员后台维护资源。

核心三件事：**前台销售、支付收单、CDN 开通中转**。

### 1.2 系统架构

```
┌────────────────┐       ┌──────────────────────────────────┐       ┌─────────────┐
│  Vue 用户门户   │ ──→  │   Laravel 后端 + Inertia 控制台    │ ←──→ │  CDNfly     │
│  (frontend)    │       │   (tycdn-backend)                 │       │  (上游 CDN)  │
│  注册/登录/购买 │       │   • API:   /api/auth, /api/orders  │       └─────────────┘
│                │       │   • 控制台: /console/* (Inertia)   │              ▲
└────────────────┘       │   • 代理:   /api/cdn, /api/admin   │              │
                          │   • 服务层: Provision/Reconcile    │       强制白名单+加密
                          └──┬───────────────────────┬───────┘              │
                             │                       │                       │
                             ▼                       ▼                       │
                       ┌───────────┐          ┌──────────────┐               │
                       │  EPUSDT   │          │  MySQL/Redis │               │
                       │ (USDT 支付)│          │              │               │
                       └───────────┘          └──────────────┘               │
                                                                              │
                       Laravel 永远在用户和 CDNfly 之间做代理 ───────────────┘
```

详细数据流见 §5（业务流程）。

### 1.3 技术选型与理由

每一行的"为什么是它"是设计决策的依据：

| 层 | 选什么 | 为什么是这个 |
|---|---|---|
| 用户门户 | Vue 3 + Vite + Arco Design | 独立 SPA，门户和控制台部署解耦；营销页可以独立迭代而不重启后端 |
| 控制台 | Laravel 13 + **Inertia.js** | Inertia 让后端控制器直接渲染 Vue 页面，**省掉单独的前端服务**——管理后台不需要再起一个 SPA 项目 |
| 认证 | Sanctum (SPA Cookie) + Fortify | Fortify 开箱即用提供登录/注册/2FA/邮箱验证；Sanctum 用 SPA Cookie 而非 token——和门户同源 cookie，简化跨子域 |
| 支付 | EPUSDT（独立服务） | 收单网关解耦——本项目只做"调网关 + 处理回调"，不碰链上签名 |
| CDN | **CDNfly + 代理层** | 不直连——前端调本项目的 `/api/cdn/*`，后端注入凭证、做白名单后转发 |
| 数据库 | MySQL 8.0（本机 / Docker 容器） | 本机开发装 MySQL；生产用项目 docker-compose 自带容器；utf8mb4 应对中文 |
| 异步 | Laravel Queue（DB 或 Redis） | 邮件、对账等异步任务 |
| 部署 | Docker Compose（8 服务） | 单文件起所有依赖，含 MySQL/Redis/EPUSDT 二进制 |

### 1.4 五个关键设计模式

#### ① 双前端架构

门户和控制台**故意分开**：门户是营销面（含 SEO 友好的注册流程），控制台是登录后的资源管理。

- 门户：Vue SPA + Vite，独立部署在 `frontend/`
- 控制台：Inertia，后端直接渲染 Vue 页面，代码在 `tycdn-backend/resources/js/Pages/`
- 共享：同一个 Laravel 后端、同一套 Sanctum Session

#### ② CDN 代理层（不直连上游）

前端**永远不直接调 CDNfly**。所有调用走代理 → `CdnflyApiService` → CDNfly。代理做三件事：

- **字段白名单**（`app/Support/CdnflyRequestGuard.php`）：用户能传什么字段写死
- **凭证注入**：用户的 CDNfly API Key/Secret 在后端用独立密钥（`CDNFLY_ENCRYPTION_KEY`，与 `APP_KEY` 解耦）解密注入
- **出站开关**：`CDNFLY_OUTBOUND_ENABLED=false` 时整个出站链路阻断（本地开发用）

#### ③ 支付三层幂等

回放/重复回调是支付系统的核心风险。三层 unique 索引防重（见 §7.4）：

- `orders.gateway_trade_id` —— EPUSDT 交易号唯一
- `payment_transactions.txid` —— 链上 hash 唯一
- `service_instances.source_order_id` —— 同一订单不能开两次服务

回调入口 `Api/EpusdtController::notify` 用 **签名校验 + `lockForUpdate` + trade_id 去重** 三步收口。

#### ④ 服务端计价 + 订单状态机

订单金额**禁止前端传**（早期漏洞，已修），`Api/OrderController::store` 服务端计算 `amount_usdt`。订单走 5 态状态机：

```
pending  ──→  paid  ──→  provisioning  ──→  active
   │                          │
   └────→  cancelled  ←───────┘
```

#### ⑤ 漏回调自愈（对账兜底）

EPUSDT 网络抽风时回调可能丢。`payments:reconcile` 命令每 5 分钟跑，主动去 EPUSDT 查单兜底——**但依赖一个待配置的 env**（`EPUSDT_QUERY_ORDER_PATH`，目前空着，见 §6.2）。

### 1.5 安全设计要点

| 维度 | 措施 |
|---|---|
| 认证 | Sanctum Token 30 天过期 + CSRF + 419 自动重试 + redirect 白名单（防开放跳转） |
| 鉴权 | 业务接口全部挂 `verified` 中间件（强制邮箱验证）；订单按 `user_id` 隔离；管理路径敏感前缀收紧 |
| 凭证 | CDNfly 凭证独立加密（`CDNFLY_ENCRYPTION_KEY`）；日志脱敏（`SENSITIVE_KEYS` 黑名单）；CDNfly URL 强制 HTTPS |
| 限流 | 全部认证端点 + 管理写操作有 throttle |
| 管理员保护 | 不能删自己 / 至少保留一个管理员 / 创建用户必走验证邮件 |
| 支付 | 服务端计价 + 三层幂等 + 签名校验 + 异常脱敏 |

### 1.6 部署形态

`docker-compose.yml` 编排 8 服务：`app`（Laravel-FPM）+ `queue` + `scheduler` + `nginx`（80 API / 8080 门户）+ `portal-build`（一次性构建 Vue 门户）+ `mysql` + `redis` + `epusdt`。生产前面再套一层宿主机 Nginx 做 SSL 终止。

完整部署细节见 §8。

### 1.7 当前阶段

代码层面**已达商业可用**（通过 2026-05-16 审计），但**未上线**。剩：
- **8 项 H/M 加固**（CDNfly 重试、审计日志、异常脱敏、管理员强制 2FA 等）
- **6 项部署期配置**（凭据轮换、reCAPTCHA、EPUSDT 查单路径、CORS、SESSION_SECURE_COOKIE）
- **用户端业务流程补全**（开通套餐 → 添加网站 → 站点配置 这条链路未接通 CDNfly）

详见 §6.2。

### 1.8 阅读本文档的指引

| 你的关注点 | 跳到哪一章 |
|---|---|
| 我想先把项目跑起来 | §4 本地启动 |
| 我想懂业务怎么流转 | §5 业务流程 |
| 我想知道做了什么 / 没做什么 | §6 项目当前状态 |
| 我想看数据库结构 | §7 数据库 |
| 我要部署上线 | §8 生产部署 |
| 我要查某个文件在哪 | §9 文档与代码索引 |

---

## 2. 技术栈与关键术语

### 2.1 技术栈速览

| 部位 | 技术 | 一句话解释 |
|---|---|---|
| 用户门户 | **Vue 3** + **Vite** + **Arco Design** | Vue 是前端 JS 框架（类似 React），Vite 是开发服务器，Arco 是组件库 |
| 后端 + 管理控制台 | **Laravel 13** + **Inertia.js** | Laravel 是 PHP Web 框架（路由、ORM、队列一站式），Inertia 让 Laravel 直接渲染 Vue 页面而不用单独再起一个前端 |
| 认证 | **Sanctum** + **Fortify** | Sanctum 颁发 Token / 维护 Session，Fortify 提供登录/注册/2FA 等开箱即用的路由 |
| 数据库 | **MySQL 8.0** | 本机开发自己装 MySQL 接 127.0.0.1:3306；生产用项目 docker-compose 自带 MySQL 容器。`.env.example` 默认值是 sqlite 但本项目实际用 mysql |
| 支付 | **EPUSDT** | 独立的 USDT 收款服务，Laravel 通过 HTTP API 与它交互 |
| 上游 CDN | **CDNfly** | 真正提供 CDN 能力的第三方系统，Laravel 充当代理层 |
| 异步 | **Queue** + **Scheduler** | Laravel 自带，发邮件、对账等任务走队列 |

### 2.2 推荐先读 30 分钟（接手第一天）

- Laravel 速览：<https://laravel.com/docs/13.x>（看 "Routing"、"Controllers"、"Eloquent" 三章）
- Vue 3 速览：<https://vuejs.org/guide/introduction.html>（看 "Single-File Components"）
- Inertia 速览：<https://inertiajs.com/>（看 "How It Works"，5 分钟）

### 2.3 术语速查

| 术语 | 解释 |
|---|---|
| **Inertia** | Laravel ↔ Vue 桥接层。后端控制器 `return Inertia::render('PageName', $props)` 就能渲染 Vue 页面，不需要单独的前端项目 |
| **Sanctum** | Laravel 自带的轻量认证，支持 SPA Cookie 模式和 API Token 模式；本项目主要用 SPA + Session |
| **Fortify** | Laravel 自带的"登录/注册/2FA/邮箱验证"路由套件，开箱即用 |
| **artisan** | Laravel 的命令行工具，`php artisan xxx` 调用各种命令 |
| **migration** | 数据库表结构的版本控制文件，改表结构要新建一个 migration |
| **seeder** | 给数据库填初始数据的脚本（比如默认管理员账号） |
| **CDNfly** | 上游 CDN 服务商，本项目通过 HTTP API 调用它给用户开通服务 |
| **EPUSDT** | 第三方 USDT 收款服务，独立部署，Laravel 通过 HTTP 与它交互 |
| **provision** | "开通"——用户付款成功后，调 CDNfly 给用户开通服务包的过程 |
| **outbound** | 出站，`CDNFLY_OUTBOUND_ENABLED=false` 时本系统不会真的去调 CDNfly |

---

## 3. 仓库目录地图

```
/
├── frontend/                       # Vue 用户门户（首页、登录、注册、2FA）
│   ├── src/
│   │   ├── views/                  # 页面组件（每个 .vue 文件是一个页面）
│   │   ├── router/                 # 前端路由表
│   │   ├── api/                    # 封装后端 HTTP 调用（含 http.js CSRF 自动重试）
│   │   └── config/                 # 运行时配置（runtime.js 含 resolvePostAuthRedirect）
│   ├── .env.example
│   ├── package.json
│   └── vite.config.js
│
├── tycdn-backend/                  # Laravel 后端 + 管理控制台
│   ├── app/
│   │   ├── Http/Controllers/       # HTTP 请求处理器（业务全部在 Api/ 子目录）
│   │   │   ├── Api/AuthController.php          # 注册/登录/邮箱验证
│   │   │   ├── Api/OrderController.php         # 下单
│   │   │   ├── Api/AdminController.php         # 管理员对用户/资源的增删改
│   │   │   ├── Api/AdminFinanceController.php  # 财务管理
│   │   │   ├── Api/CdnProxyController.php      # 用户侧 CDNfly 代理（白名单）
│   │   │   ├── Api/EpusdtController.php        # 支付回调入口
│   │   │   ├── Api/AdminSiteController.php / AdminNodeController.php / ...
│   │   │   └── Auth/ForgotPasswordController.php  # 找回密码（web 路由）
│   │   ├── Models/                 # 数据库模型
│   │   ├── Services/               # 业务逻辑层
│   │   │   ├── CdnflyApiService.php             # CDNfly HTTP 客户端
│   │   │   ├── CdnflyProvisionService.php       # 支付成功后开通服务
│   │   │   ├── EpusdtService.php                # EPUSDT API 封装
│   │   │   ├── EpusdtCheckoutService.php        # 创建支付订单
│   │   │   ├── PaymentReconciliationService.php # 漏回调对账
│   │   │   └── RecaptchaService.php             # reCAPTCHA 校验
│   │   ├── Support/CdnflyRequestGuard.php       # 代理字段白名单
│   │   └── Console/                # artisan 命令（cdnfly:* / payments:reconcile）
│   ├── routes/
│   │   ├── api.php                 # API 路由总入口（接手先读这个）
│   │   ├── web.php                 # web 路由（Inertia 页面、找回密码）
│   │   └── console.php             # 定时任务调度
│   ├── resources/js/Pages/         # 管理控制台 Vue 页面（Inertia 渲染）
│   ├── database/migrations/        # 数据库表结构（按时间顺序）
│   ├── config/                     # Laravel 配置文件（sanctum/fortify/cors 等）
│   ├── docker-compose.yml          # 生产部署编排
│   ├── .env.example
│   └── composer.json
│
├── docs/                           # CDNfly 各模块设计文档
│   ├── CC规则结构化表单方案.md      # CC 规则的表单结构
│   ├── Certs.pdf                   # 证书管理（CDNfly 后台设计）
│   ├── Node-groups.pdf             # 节点组
│   ├── SiteManange.pdf             # 网站管理
│   ├── Sitegroups.pdf              # 站点组
│   ├── Streamgroups.pdf            # 四层组
│   ├── Streams.pdf                 # 四层流
│   ├── Userconfigs.pdf             # 用户配置
│   └── packages/                   # 套餐设计资料
│
├── 上线前运维.md                    # 上线前必做清单（含真实凭据值，⚠️ 不能外传）
├── 待办.md                          # 待办主索引（每天更新，权威来源）
├── 审查报告.md                      # 安全审查报告
├── 交接文档.md                      # 私人凭据本（含 CDNfly/腾讯云/邮箱真实账密，⚠️ 已 .gitignore，不能外传）
└── 开发交接.md                      # 本文档（项目交接总索引）
```

---

## 4. 本地启动

### 4.1 前置环境

| 工具 | 版本 | 安装建议（Windows） |
|---|---|---|
| **PHP** | 8.3+ | Laragon / XAMPP，或单独安装 PHP |
| **Composer** | 2.x | <https://getcomposer.org/download/> |
| **Node.js** | 18+ | <https://nodejs.org/>（含 npm） |
| **Git** | 任意 | 可选（当前项目不是 git 仓库，靠本地备份） |

### 4.2 启动后端（Laravel）

```bash
cd tycdn-backend
composer install                    # 安装 PHP 依赖（首次）
cp .env.example .env                # 复制环境变量模板
php artisan key:generate            # 生成 APP_KEY
php artisan migrate                 # 创建数据库表（先按 §8.3.2 装好 MySQL 并建库）
php artisan db:seed                 # 创建默认管理员（用 .env 里的 ADMIN_* 值）
composer run dev                    # 一条命令同时跑 Laravel / queue worker / Vite HMR
```

- 后端 API + 控制台：**http://localhost:8013**
- Vite HMR（管理控制台前端热更新）：**5173**

### 4.3 启动前端门户（Vue）

另开一个终端：

```bash
cd frontend
npm install                         # 安装 npm 依赖（首次）
cp .env.example .env                # 默认指向 localhost:8013
npm run dev                         # 启动 Vite 开发服务器
```

- 用户门户：**http://localhost:5177**

### 4.4 首次登录

- 管理员：用 `tycdn-backend/.env` 里的 `ADMIN_EMAIL` / `ADMIN_PASSWORD`（本地默认是弱密码，上线前必须改）
- 普通用户：通过门户注册流程创建；开发环境邮件验证链接会写进日志 `tycdn-backend/storage/logs/laravel.log`

### 4.5 常见报错

| 现象 | 原因 | 处理 |
|---|---|---|
| `Class not found` / `vendor not exists` | 没装 PHP 依赖 | `composer install` |
| 前端 `Cannot find module` | 没装 npm 依赖 | `npm install` |
| 接口报 **419 Unknown Status** | CSRF token 失效 | 浏览器清 cookie 重登；代码层已加自动重试 |
| 数据库报 `SQLSTATE` 或表不存在 | 没跑 migrate | `php artisan migrate:fresh --seed`（**会清空数据**） |
| 调用 CDN 接口返回空数据 / 写操作被阻断 | 本地默认禁用 CDNfly 出站 | 这是预期行为；需要联通真实 CDNfly 时设 `CDNFLY_OUTBOUND_ENABLED=true` 并重启 |
| 端口被占用 | 5177 / 8013 / 5173 被占 | 改 `.env` 端口，或关掉占用进程 |

本地启动 / CNAME 域名映射 / 恢复 CDNfly 出站等更多细节，参见 §5、§7、§8.9。

---

## 5. 业务流程与核心代码路径

### 5.1 登录流程

```
frontend/src/views/LoginView.vue
  ↓ 提交表单
tycdn-backend/routes/api.php   (POST /api/auth/login)
  ↓
app/Http/Controllers/Api/AuthController.php::login()
  ↓ 验证账密 + 颁 Sanctum Token
返回前端
  ↓
frontend/src/config/runtime.js::resolvePostAuthRedirect()  // origin 白名单校验
  ↓
跳转到 /dashboard 或 ?redirect=... 指定的安全路径
```

### 5.2 支付流程

```
用户在控制台点购买
  ↓
Api/OrderController::store()         // ⚠ 服务端计价，禁止前端传金额
  ↓
EpusdtCheckoutService::create()      // 用 Str::ulid() 生成订单号
  ↓ 调 EPUSDT 创建支付
跳转 EPUSDT 收银台
  ↓ 用户付款
EPUSDT 回调 → Api/EpusdtController::notify()
  ↓ 签名校验 + lockForUpdate + trade_id 去重（幂等）
订单状态置为 paid → CdnflyProvisionService 调 CDNfly 开通
```

漏回调兜底由 `payments:reconcile` 命令（每 5 分钟）处理 —— 但**前提是配了 `EPUSDT_QUERY_ORDER_PATH`**（见 §6.2）。

### 5.3 CDNfly 代理流程

```
[用户侧]
frontend → /api/cdn/* → Api/CdnProxyController（白名单路径 + 字段过滤）→ CdnflyApiService → CDNfly

[管理侧]
console → /api/admin/* → Api/AdminController / Api/AdminSiteController / ... → CdnflyApiService → CDNfly
```

`CdnflyApiService` 已强制 HTTPS、日志脱敏（`SENSITIVE_KEYS` 常量）、独立加密密钥。**本地默认禁用出站**：`CDNFLY_OUTBOUND_ENABLED=false` 时读接口返回空、写接口被阻断。

---

## 6. 项目当前状态

> **完整明细见 [`待办.md`](待办.md)**（权威源，每天更新）。本节是高层导航。

### 6.1 已完成的关键工作

**认证体系**
- 注册 / 登录 / 邮箱验证强制（业务接口全部挂 `verified` 中间件）
- 2FA、找回密码（统一成功响应，避免邮箱枚举）
- Sanctum Token 设 30 天过期（`SANCTUM_EXPIRATION=43200` 分钟）
- 登录 / 2FA 成功后通过 `resolvePostAuthRedirect()` 做 redirect 白名单校验

**支付链路**
- EPUSDT 创建订单：**服务端计价**，禁止前端传 `user_id`（已修复早期严重漏洞）
- 回调：签名校验 + `lockForUpdate` + `trade_id` 去重，幂等可重放
- 订单号改用 `Str::ulid()` 消除碰撞
- 异常响应已脱敏
- Tron 直付链路已**整体移除**（不要再尝试恢复）
- 漏回调对账：`payments:reconcile` 每 5 分钟运行

**管理后台**（11 个模块 CRUD 已就绪）
管理概览 / 用户 / 套餐 / 网站 / 节点 / DNS / 四层转发 / 财务 / 监控日志 / 系统配置 / 安全权限。所有删除走 `ConfirmDeleteDialog`（替换 35 个页面的 `window.confirm`），成功提示统一走 `toast.success()`（替换 15 个页面）。

**安全加固**
- 订单 / 支付接口全部认证 + 归属鉴权
- 用户侧 CDNfly 代理改为**字段白名单**（`CdnflyRequestGuard`）
- 管理侧代理收紧敏感路径（如 `/v1/users`）
- CDNfly 凭证独立加密（`CDNFLY_ENCRYPTION_KEY`，与 `APP_KEY` 解耦）
- 全部认证端点 + 管理写操作有 throttle 限流
- 419 CSRF 失败自动复位 + 重试
- 管理员自我保护：不能删除自己 / 至少保留一个管理员

**2026-05-16 商业可用性审计** C 级（C1–C4）全部已完成：Sanctum 过期时间、API 强制邮箱验证、管理员创建用户走验证邮件、中文乱码修复。

### 6.2 未完成（按优先级）

#### ⚠️ 部署期必须做（代码已就绪，缺配置）

| 优先级 | 项 | 涉及文件 | 不做的后果 |
|---|---|---|---|
| **P0** | 凭据轮换：当前 `.env` 中 8 个字段是弱密码或乱码 | `tycdn-backend/.env` | 上线即被攻破（详见 §8.8） |
| **P0** | `APP_ENV=production`、`APP_DEBUG=false` | `.env` | 调试模式泄露代码/配置 |
| **M3** | `SESSION_SECURE_COOKIE=true` | `.env` | HTTPS 下 cookie 可被嗅探 |
| **P2** | reCAPTCHA v2 密钥 | `.env`、`frontend/.env` | 注册/登录/找回密码三处无防刷 |
| **P2** | `EPUSDT_QUERY_ORDER_PATH` | `.env`、`PaymentReconciliationService.php` | 漏回调无法自动对账 |
| **P2** | `CORS_ALLOWED_ORIGINS` 收紧 | `.env`、`config/cors.php` | 任意 origin 可跨域调用 |

#### 代码加固（2026-05-16 商业审计剩余 8 项 H/M）

| 优先级 | 项 | 文件 |
|---|---|---|
| H1 | CDNfly HTTP 调用加 retry + backoff | `app/Services/CdnflyApiService.php` |
| H2 | `ensureOutboundEnabled` 改成业务错误（503）而不是抛 `RuntimeException`（500） | `CdnflyApiService.php:114-124` |
| H3 | 管理员写操作加**结构化审计日志** | `Api/AdminController.php` 所有 store/update/destroy |
| H4 | `parseCreateTransactionResponse` 异常脱敏 | `EpusdtService.php:150,154,159,164` |
| M1 | 管理员强制 2FA | `config/fortify.php`、`Api/AdminController.php` |
| M2 | 订单过期时间可配置 | `EpusdtCheckoutService.php:73`（硬编码 30 分钟） |
| M4 | reCAPTCHA 密钥配置后端到端验证三处端点 | 见上方部署期 |

#### 架构清理（不紧急但建议早做）

> ✅ "Fortify 登录改 API 实现" 和 "2FA / 密码重置路由 API 化" 已完成（见 [`待办.md`](待办.md) 架构清理小节）。下面是仍待清理的项：

- **`APP_PREVIOUS_KEYS` 残留引用清理**：密钥兼容轮换期已结束（当前 `.env` 里值为空），但代码里还在引用，建议清干净：
  - [`tycdn-backend/.env.example:9`](tycdn-backend/.env.example) —— 删 `APP_PREVIOUS_KEYS=` 这一行
  - [`tycdn-backend/config/app.php:103-104`](tycdn-backend/config/app.php) —— 删 `previous_keys` 读取段
  - [`tycdn-backend/.env:9`](tycdn-backend/.env) —— 删除该字段
- **前端路由 `beforeEach` 守卫**：[`frontend/src/router/index.js`](frontend/src/router/index.js) **没有任何**前端访问控制（依赖后端 401 拦截），属于纵深防御缺口

#### 产品功能补全（不是 bug，是没做完）

- **用户端核心流程未通**：开通套餐 → 添加网站 → 站点配置，**仍未接通真实 CDNfly 接口**（管理端已接通）
- 上线前替换控制台 / 邮件模板 / Favicon 的**品牌 Logo**
- `/verify-email` 页面对已验证用户应直接跳 `/dashboard`
- 把页面里残留的"部署说明"文案彻底移到文档
- 邮件发送频控**外部告警**（已有日志预警，缺 Slack/邮件通道）

---

## 7. 数据库

> 全部表结构来自 [`tycdn-backend/database/migrations/`](tycdn-backend/database/migrations) 里 20 个 migration 文件。`php artisan migrate` 会按时间戳顺序建表 + 加列 + 加索引 + 加外键。**最后一个 `2026_04_22_100002_add_foreign_keys.php` 专门加外键约束，不要单独跳过**。

### 7.1 实体关系总览

业务核心围绕"订单"展开，从用户下单到 CDNfly 上真实创建服务实例：

```
                       ┌──────────────────────────┐
                       │      users (用户)         │
                       │  含 CDNfly 凭证 / 2FA     │
                       └──────────────┬───────────┘
                                      │ user_id
                ┌─────────────────────┼─────────────────────┐
                │                     │                     │
                ▼                     ▼                     ▼
       ┌─────────────────┐  ┌─────────────────┐  ┌────────────────────┐
       │  orders (订单)  │  │ service_        │  │ personal_access_   │
       │  含 EPUSDT 网关 │  │ instances       │  │ tokens (Sanctum)   │
       │  字段、状态机   │  │  (CDN 实例)     │  └────────────────────┘
       └────────┬────────┘  └────────┬────────┘
                │ product_id          ▲ source_order_id
                ▼                     │
       ┌─────────────────┐            │
       │ products (套餐) │            │
       └────────┬────────┘            │
                │ 1:1                 │
                ▼                     │
       ┌──────────────────────────┐   │
       │ product_cdnfly_mappings  │   │
       │  (本地套餐↔CDNfly 套餐)  │   │
       └──────────────────────────┘   │
                                      │
       ┌─────────────────────────────┴───────┐
       │     payment_transactions             │
       │     (链上交易明细，order_id 外键)    │
       └──────────────────────────────────────┘
```

**数据流**：用户下单 → `orders.status=pending` → EPUSDT 回调写入 `payment_transactions` → `orders.status=paid` → `CdnflyProvisionService` 在 CDNfly 创建服务 → 写入 `service_instances` → `orders.status=active`。

### 7.2 业务核心表（6 张）

#### users —— 用户主表

存储认证信息、2FA、CDNfly 凭证、角色。

| 字段 | 类型 | 约束 / 备注 |
|---|---|---|
| `id` | bigint unsigned | 主键，自增 |
| `name` | string(255) | 用户名 |
| `role` | string(20) | 默认 `user`；取值 `admin` / `user` |
| `email` | string(255) | **unique** |
| `email_verified_at` | timestamp | nullable |
| `password` | string(255) | bcrypt 哈希 |
| `two_factor_secret` | text | 2FA TOTP secret，nullable |
| `two_factor_recovery_codes` | text | 2FA 恢复码，nullable |
| `two_factor_confirmed_at` | timestamp | nullable |
| `cdnfly_user_id` | bigint unsigned | nullable |
| `cdnfly_api_key` | text | 加密存储，nullable |
| `cdnfly_api_secret` | text | 加密存储，nullable |
| `cdnfly_synced_at` | timestamp | nullable |
| `remember_token` | string(100) | nullable |
| `created_at` / `updated_at` | timestamp | Laravel 自动维护 |

- **索引**：unique `(email)`；index `(cdnfly_user_id)`、`(role)`
- **外键**：无
- **注**：`cdnfly_api_key/secret` 在 2026-04-24 从 `string(64)` 扩成 `text`（密文更长）

#### products —— CDN 套餐定义

| 字段 | 类型 | 约束 / 备注 |
|---|---|---|
| `id` | bigint unsigned | 主键 |
| `name` | string(100) | 套餐名 |
| `slug` | string(100) | **unique** |
| `description` | text | nullable |
| `price_monthly` / `price_quarterly` / `price_yearly` | decimal(10,2) | 默认 0 |
| `currency` | string(10) | 默认 `USD` |
| `is_active` | boolean | 默认 true |
| `sort_order` | integer | 默认 0 |
| `features` | json | 前端展示用，nullable |
| `created_at` / `updated_at` | timestamp | — |

- **索引**：unique `(slug)`；index `(is_active)`
- **外键**：无

#### product_cdnfly_mappings —— 本地套餐 ↔ CDNfly 套餐 1:1 绑定

| 字段 | 类型 | 约束 / 备注 |
|---|---|---|
| `id` | bigint unsigned | 主键 |
| `product_id` | bigint unsigned | **unique**，外键 → `products(id) RESTRICT` |
| `cdnfly_plan_id` | string(64) | nullable |
| `cdnfly_group_id` | string(64) | nullable |
| `provision_payload` | json | 开通时下发给 CDNfly 的固定负载，nullable |
| `created_at` / `updated_at` | timestamp | — |

- **索引**：unique `(product_id)`；index `(cdnfly_plan_id)`、`(cdnfly_group_id)`

#### orders —— 订单主表（字段最多）

| 字段 | 类型 | 约束 / 备注 |
|---|---|---|
| `id` | bigint unsigned | 主键 |
| `order_no` | string(32) | **unique**，应用层 `Str::ulid()` 生成 |
| `user_id` | bigint unsigned | 外键 → `users(id) SET NULL`，nullable |
| `product_id` | bigint unsigned | 外键 → `products(id) SET NULL`，nullable |
| `target_service_instance_id` | bigint unsigned | 升级/续费目标，外键 → `service_instances(id) SET NULL`，nullable |
| `amount_usdt` | decimal(18,6) | 服务端计价 |
| `fiat_amount` | decimal(18,2) | nullable |
| `fiat_currency` | string(10) | 默认 `USD` |
| `fx_rate_snapshot` | decimal(18,6) | 下单时汇率快照，nullable |
| `pay_address` | string(64) | 收款地址 |
| `pay_currency` | string(20) | 默认 `USDT_TRC20` |
| `actual_paid_amount` | decimal(18,6) | nullable |
| `status` | string(20) | **状态机**，默认 `pending`；取值 `pending` / `paid` / `provisioning` / `active` / `cancelled` |
| `order_type` | string(20) | 默认 `new`；取值 `new` / `renew` / `upgrade` |
| `billing_cycle` | string(20) | `monthly` / `quarterly` / `yearly`，nullable |
| `quantity` | integer unsigned | 默认 1 |
| `gateway_provider` | string(20) | nullable |
| `gateway_trade_id` | string(64) | **unique**，幂等去重键，nullable |
| `gateway_request_id` / `gateway_token` | string(64) | nullable |
| `gateway_currency` | string(10) | nullable |
| `gateway_amount` / `gateway_actual_amount` | decimal(19,4) | nullable |
| `gateway_payment_url` | string(255) | 收银台跳转，nullable |
| `gateway_status` | string(20) | nullable |
| `gateway_expired_at` | timestamp | nullable |
| `gateway_notify_payload` | json | 回调原始数据存档，nullable |
| `expire_at` | timestamp | 订单过期时间（硬编码 30 分钟，见 §6.2 M2） |
| `paid_at` / `provisioned_at` / `cancelled_at` | timestamp | nullable |
| `created_at` / `updated_at` | timestamp | — |

- **索引**：
  - unique `(order_no)`、unique `(gateway_trade_id)`
  - 复合 index `(status, expire_at)` —— 对账 / 过期清理用
  - index `(pay_address)`、`(user_id)`、`(product_id)`、`(target_service_instance_id)`、`(gateway_provider)`、`(gateway_status)`
- **外键**：`user_id` / `product_id` / `target_service_instance_id` 全部 `SET NULL`

#### payment_transactions —— 链上交易明细

| 字段 | 类型 | 约束 / 备注 |
|---|---|---|
| `id` | bigint unsigned | 主键 |
| `order_id` | bigint unsigned | 外键 → `orders(id) RESTRICT` |
| `chain` | string(20) | 默认 `TRON` |
| `token` | string(20) | 默认 `USDT` |
| `txid` | string(128) | **unique**，链上交易哈希 |
| `from_address` / `to_address` | string(64) | — |
| `amount` | decimal(18,6) | — |
| `block_timestamp` | bigint unsigned | 区块时间 |
| `confirmations` | integer unsigned | 默认 0 |
| `status` | string(20) | 默认 `confirmed`；取值 `pending` / `confirmed` / `failed` |
| `confirmed_at` / `processed_at` | timestamp | nullable |
| `raw_payload` | json | 链上原始数据，nullable |
| `created_at` / `updated_at` | timestamp | — |

- **索引**：unique `(txid)`；index `(order_id)`
- **外键**：`order_id → orders(id) RESTRICT`

#### service_instances —— CDN 服务实例

支付成功后开通生成，这是真正"用户买到的东西"。

| 字段 | 类型 | 约束 / 备注 |
|---|---|---|
| `id` | bigint unsigned | 主键 |
| `user_id` | bigint unsigned | 外键 → `users(id) SET NULL`，nullable |
| `product_id` | bigint unsigned | nullable（**未加外键**，软关联） |
| `source_order_id` | bigint unsigned | **unique**，外键 → `orders(id) SET NULL`，nullable |
| `status` | string(20) | 默认 `pending`；取值 `pending` / `provisioning` / `active` / `suspended` / `expired` / `failed` |
| `cdnfly_user_id` / `cdnfly_service_id` | string(64) | nullable |
| `service_name` | string(100) | nullable |
| `opened_at` / `expired_at` | timestamp | nullable |
| `config_snapshot` | json | 开通时配置快照，nullable |
| `extra` | json | 扩展字段，nullable |
| `created_at` / `updated_at` | timestamp | — |

- **索引**：unique `(source_order_id)`；index `(user_id)`、`(product_id)`、`(cdnfly_user_id)`、`(cdnfly_service_id)`、`(status)`
- **外键**：`user_id → users(id) SET NULL`、`source_order_id → orders(id) SET NULL`

### 7.3 框架 / 基础设施自动表（8 张）

这些是 Laravel 自带或被项目 driver 配置触发创建的，**没有业务字段**，但生产数据库里会有，**不要手动删**。

| 表 | 触发来源 | 一句话作用 |
|---|---|---|
| `password_reset_tokens` | Laravel 默认 | 找回密码 token（email 为主键） |
| `sessions` | `SESSION_DRIVER=database` | Web 会话存储 |
| `cache` | `CACHE_STORE=database` | 应用缓存键值对 |
| `cache_locks` | 同上 | 分布式缓存锁 |
| `jobs` | `QUEUE_CONNECTION=database` | 待执行的队列任务 |
| `job_batches` | 同上 | 任务批次跟踪 |
| `failed_jobs` | 同上 | 失败任务 + 异常堆栈 |
| `personal_access_tokens` | Sanctum | API token（多态关联，本项目主要走 SPA Session） |

> **若切到 Redis**：`cache` / `cache_locks` / `jobs` / `job_batches` / `failed_jobs` 表就用不到了（数据走 Redis），但表结构因 migrate 仍会存在，无害。`sessions` 表仍要保留，除非把 `SESSION_DRIVER` 也改成 `redis`。

### 7.4 关键约束行为速记

- **删用户**：`orders.user_id` 和 `service_instances.user_id` 都是 `SET NULL`（订单/实例保留，便于审计与对账），不要 cascade
- **删订单**：被 `payment_transactions.order_id` `RESTRICT` —— 有交易记录的订单**删不掉**，这是故意的
- **删产品**：`orders.product_id` 是 `SET NULL`；`product_cdnfly_mappings.product_id` 是 `RESTRICT`（先解绑映射再删产品）
- **删服务实例**：`orders.target_service_instance_id` 是 `SET NULL`
- **幂等防重三层**：`orders.gateway_trade_id` + `payment_transactions.txid` + `service_instances.source_order_id` 全部 unique

### 7.5 建表 SQL（MySQL 方言）

> 以下是 `php artisan migrate` 在 MySQL 实际生成的等效 SQL（合并了多次增量 migration）。
>
> ⚠️ **生产部署不要手动跑这些 SQL** —— 用 `php artisan migrate --force` 让 Laravel 按顺序跑 20 个 migration 文件，框架才能知道当前 schema 版本号。下面 SQL 仅供阅读 / DBA 审计 / 数据迁移参考。
>
> 字段顺序遵循实际 migration 执行后的物理排列（受 `->after('...')` 影响）。

#### users

```sql
CREATE TABLE `users` (
  `id` bigint unsigned NOT NULL AUTO_INCREMENT,
  `name` varchar(255) NOT NULL,
  `role` varchar(20) NOT NULL DEFAULT 'user',                   -- admin / user
  `email` varchar(255) NOT NULL,
  `email_verified_at` timestamp NULL DEFAULT NULL,
  `password` varchar(255) NOT NULL,
  `cdnfly_user_id` bigint unsigned DEFAULT NULL,
  `cdnfly_api_key` text,                                         -- 加密后密文（2026-04-24 从 varchar(64) 扩成 text）
  `cdnfly_api_secret` text,                                      -- 加密后密文
  `cdnfly_synced_at` timestamp NULL DEFAULT NULL,
  `two_factor_secret` text,                                      -- TOTP secret
  `two_factor_recovery_codes` text,
  `two_factor_confirmed_at` timestamp NULL DEFAULT NULL,
  `remember_token` varchar(100) DEFAULT NULL,
  `created_at` timestamp NULL DEFAULT NULL,
  `updated_at` timestamp NULL DEFAULT NULL,
  PRIMARY KEY (`id`),
  UNIQUE KEY `users_email_unique` (`email`),
  KEY `users_cdnfly_user_id_index` (`cdnfly_user_id`),
  KEY `users_role_index` (`role`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### products

```sql
CREATE TABLE `products` (
  `id` bigint unsigned NOT NULL AUTO_INCREMENT,
  `name` varchar(100) NOT NULL,
  `slug` varchar(100) NOT NULL,
  `description` text,
  `price_monthly` decimal(10,2) NOT NULL DEFAULT '0.00',
  `price_quarterly` decimal(10,2) NOT NULL DEFAULT '0.00',
  `price_yearly` decimal(10,2) NOT NULL DEFAULT '0.00',
  `currency` varchar(10) NOT NULL DEFAULT 'USD',
  `is_active` tinyint(1) NOT NULL DEFAULT '1',
  `sort_order` int NOT NULL DEFAULT '0',
  `features` json DEFAULT NULL,                                  -- 前端展示用的套餐特性
  `created_at` timestamp NULL DEFAULT NULL,
  `updated_at` timestamp NULL DEFAULT NULL,
  PRIMARY KEY (`id`),
  UNIQUE KEY `products_slug_unique` (`slug`),
  KEY `products_is_active_index` (`is_active`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### product_cdnfly_mappings

```sql
CREATE TABLE `product_cdnfly_mappings` (
  `id` bigint unsigned NOT NULL AUTO_INCREMENT,
  `product_id` bigint unsigned NOT NULL,
  `cdnfly_plan_id` varchar(64) DEFAULT NULL,
  `cdnfly_group_id` varchar(64) DEFAULT NULL,
  `provision_payload` json DEFAULT NULL,                         -- 开通时下发给 CDNfly 的固定负载
  `created_at` timestamp NULL DEFAULT NULL,
  `updated_at` timestamp NULL DEFAULT NULL,
  PRIMARY KEY (`id`),
  UNIQUE KEY `product_cdnfly_mappings_product_id_unique` (`product_id`),
  KEY `product_cdnfly_mappings_cdnfly_plan_id_index` (`cdnfly_plan_id`),
  KEY `product_cdnfly_mappings_cdnfly_group_id_index` (`cdnfly_group_id`),
  CONSTRAINT `product_cdnfly_mappings_product_id_foreign`
    FOREIGN KEY (`product_id`) REFERENCES `products` (`id`) ON DELETE RESTRICT
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### orders

订单是字段最多的一张（30+ 列），核心字段 + 法币 + EPUSDT 网关三块组合。

```sql
CREATE TABLE `orders` (
  `id` bigint unsigned NOT NULL AUTO_INCREMENT,
  `order_no` varchar(32) NOT NULL,                               -- 应用层 Str::ulid() 生成
  `user_id` bigint unsigned DEFAULT NULL,
  `product_id` bigint unsigned DEFAULT NULL,
  `order_type` varchar(20) NOT NULL DEFAULT 'new',               -- new / renew / upgrade
  `billing_cycle` varchar(20) DEFAULT NULL,                      -- monthly / quarterly / yearly
  `quantity` int unsigned NOT NULL DEFAULT '1',
  `target_service_instance_id` bigint unsigned DEFAULT NULL,     -- 升级/续费目标
  `fiat_amount` decimal(18,2) DEFAULT NULL,
  `fiat_currency` varchar(10) NOT NULL DEFAULT 'USD',
  `fx_rate_snapshot` decimal(18,6) DEFAULT NULL,                 -- 下单时汇率快照
  `amount_usdt` decimal(18,6) NOT NULL,                          -- 服务端计价
  `pay_currency` varchar(20) NOT NULL DEFAULT 'USDT_TRC20',
  `actual_paid_amount` decimal(18,6) DEFAULT NULL,
  `gateway_provider` varchar(20) DEFAULT NULL,                   -- EPUSDT 等
  `gateway_trade_id` varchar(64) DEFAULT NULL,                   -- 幂等去重键
  `gateway_request_id` varchar(64) DEFAULT NULL,
  `gateway_token` varchar(64) DEFAULT NULL,
  `gateway_amount` decimal(19,4) DEFAULT NULL,
  `gateway_currency` varchar(10) DEFAULT NULL,
  `gateway_actual_amount` decimal(19,4) DEFAULT NULL,
  `gateway_payment_url` varchar(255) DEFAULT NULL,               -- 收银台跳转地址
  `gateway_status` varchar(20) DEFAULT NULL,
  `gateway_expired_at` timestamp NULL DEFAULT NULL,
  `gateway_notify_payload` json DEFAULT NULL,                    -- 完整回调原始数据存档
  `pay_address` varchar(64) NOT NULL,
  `status` varchar(20) NOT NULL DEFAULT 'pending',               -- pending / paid / provisioning / active / cancelled
  `expire_at` timestamp NOT NULL,                                -- 订单过期时间（硬编码 30 分钟）
  `paid_at` timestamp NULL DEFAULT NULL,
  `provisioned_at` timestamp NULL DEFAULT NULL,
  `cancelled_at` timestamp NULL DEFAULT NULL,
  `created_at` timestamp NULL DEFAULT NULL,
  `updated_at` timestamp NULL DEFAULT NULL,
  PRIMARY KEY (`id`),
  UNIQUE KEY `orders_order_no_unique` (`order_no`),
  UNIQUE KEY `orders_gateway_trade_id_unique` (`gateway_trade_id`),
  KEY `orders_status_expire_at_index` (`status`,`expire_at`),
  KEY `orders_pay_address_index` (`pay_address`),
  KEY `orders_gateway_provider_index` (`gateway_provider`),
  KEY `orders_gateway_status_index` (`gateway_status`),
  KEY `orders_target_service_instance_id_index` (`target_service_instance_id`),
  KEY `orders_user_id_foreign` (`user_id`),
  KEY `orders_product_id_foreign` (`product_id`),
  CONSTRAINT `orders_user_id_foreign`
    FOREIGN KEY (`user_id`) REFERENCES `users` (`id`) ON DELETE SET NULL,
  CONSTRAINT `orders_product_id_foreign`
    FOREIGN KEY (`product_id`) REFERENCES `products` (`id`) ON DELETE SET NULL,
  CONSTRAINT `orders_target_service_instance_id_foreign`
    FOREIGN KEY (`target_service_instance_id`) REFERENCES `service_instances` (`id`) ON DELETE SET NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### payment_transactions

```sql
CREATE TABLE `payment_transactions` (
  `id` bigint unsigned NOT NULL AUTO_INCREMENT,
  `order_id` bigint unsigned NOT NULL,
  `chain` varchar(20) NOT NULL DEFAULT 'TRON',
  `token` varchar(20) NOT NULL DEFAULT 'USDT',
  `txid` varchar(128) NOT NULL,                                  -- 链上交易哈希
  `from_address` varchar(64) NOT NULL,
  `to_address` varchar(64) NOT NULL,
  `amount` decimal(18,6) NOT NULL,
  `block_timestamp` bigint unsigned NOT NULL,
  `confirmations` int unsigned NOT NULL DEFAULT '0',
  `confirmed_at` timestamp NULL DEFAULT NULL,
  `processed_at` timestamp NULL DEFAULT NULL,                    -- 业务侧处理完成时间
  `status` varchar(20) NOT NULL DEFAULT 'confirmed',             -- pending / confirmed / failed
  `raw_payload` json DEFAULT NULL,
  `created_at` timestamp NULL DEFAULT NULL,
  `updated_at` timestamp NULL DEFAULT NULL,
  PRIMARY KEY (`id`),
  UNIQUE KEY `payment_transactions_txid_unique` (`txid`),
  KEY `payment_transactions_order_id_index` (`order_id`),
  CONSTRAINT `payment_transactions_order_id_foreign`
    FOREIGN KEY (`order_id`) REFERENCES `orders` (`id`) ON DELETE RESTRICT
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### service_instances

```sql
CREATE TABLE `service_instances` (
  `id` bigint unsigned NOT NULL AUTO_INCREMENT,
  `user_id` bigint unsigned DEFAULT NULL,
  `product_id` bigint unsigned DEFAULT NULL,                     -- 未加外键，软关联
  `source_order_id` bigint unsigned DEFAULT NULL,
  `status` varchar(20) NOT NULL DEFAULT 'pending',               -- pending / provisioning / active / suspended / expired / failed
  `cdnfly_user_id` varchar(64) DEFAULT NULL,
  `cdnfly_service_id` varchar(64) DEFAULT NULL,
  `service_name` varchar(100) DEFAULT NULL,
  `opened_at` timestamp NULL DEFAULT NULL,
  `expired_at` timestamp NULL DEFAULT NULL,
  `config_snapshot` json DEFAULT NULL,
  `extra` json DEFAULT NULL,
  `created_at` timestamp NULL DEFAULT NULL,
  `updated_at` timestamp NULL DEFAULT NULL,
  PRIMARY KEY (`id`),
  UNIQUE KEY `service_instances_source_order_id_unique` (`source_order_id`),
  KEY `service_instances_user_id_index` (`user_id`),
  KEY `service_instances_product_id_index` (`product_id`),
  KEY `service_instances_cdnfly_user_id_index` (`cdnfly_user_id`),
  KEY `service_instances_cdnfly_service_id_index` (`cdnfly_service_id`),
  KEY `service_instances_status_index` (`status`),
  CONSTRAINT `service_instances_user_id_foreign`
    FOREIGN KEY (`user_id`) REFERENCES `users` (`id`) ON DELETE SET NULL,
  CONSTRAINT `service_instances_source_order_id_foreign`
    FOREIGN KEY (`source_order_id`) REFERENCES `orders` (`id`) ON DELETE SET NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

### 7.6 框架自动表的建表 SQL

这 8 张是 Laravel / Sanctum 标准定义，**不在 `database/migrations/` 里手动维护**（由框架基础迁移生成）。这里给出简化 SQL 仅供你知道它们长什么样：

```sql
-- 找回密码 token（email 为主键）
CREATE TABLE `password_reset_tokens` (
  `email` varchar(255) NOT NULL PRIMARY KEY,
  `token` varchar(255) NOT NULL,
  `created_at` timestamp NULL DEFAULT NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- Web 会话
CREATE TABLE `sessions` (
  `id` varchar(255) NOT NULL PRIMARY KEY,
  `user_id` bigint unsigned DEFAULT NULL,
  `ip_address` varchar(45) DEFAULT NULL,
  `user_agent` text,
  `payload` longtext NOT NULL,
  `last_activity` int NOT NULL,
  KEY `sessions_user_id_index` (`user_id`),
  KEY `sessions_last_activity_index` (`last_activity`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 应用缓存
CREATE TABLE `cache` (
  `key` varchar(255) NOT NULL PRIMARY KEY,
  `value` mediumtext NOT NULL,
  `expiration` int NOT NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE `cache_locks` (
  `key` varchar(255) NOT NULL PRIMARY KEY,
  `owner` varchar(255) NOT NULL,
  `expiration` int NOT NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 队列
CREATE TABLE `jobs` (
  `id` bigint unsigned NOT NULL AUTO_INCREMENT PRIMARY KEY,
  `queue` varchar(255) NOT NULL,
  `payload` longtext NOT NULL,
  `attempts` tinyint unsigned NOT NULL,
  `reserved_at` int unsigned DEFAULT NULL,
  `available_at` int unsigned NOT NULL,
  `created_at` int unsigned NOT NULL,
  KEY `jobs_queue_index` (`queue`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE `job_batches` (
  `id` varchar(255) NOT NULL PRIMARY KEY,
  `name` varchar(255) NOT NULL,
  `total_jobs` int NOT NULL,
  `pending_jobs` int NOT NULL,
  `failed_jobs` int NOT NULL,
  `failed_job_ids` longtext NOT NULL,
  `options` mediumtext,
  `cancelled_at` int DEFAULT NULL,
  `created_at` int NOT NULL,
  `finished_at` int DEFAULT NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE `failed_jobs` (
  `id` bigint unsigned NOT NULL AUTO_INCREMENT PRIMARY KEY,
  `uuid` varchar(255) NOT NULL,
  `connection` text NOT NULL,
  `queue` text NOT NULL,
  `payload` longtext NOT NULL,
  `exception` longtext NOT NULL,
  `failed_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,
  UNIQUE KEY `failed_jobs_uuid_unique` (`uuid`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- Sanctum API Token
CREATE TABLE `personal_access_tokens` (
  `id` bigint unsigned NOT NULL AUTO_INCREMENT PRIMARY KEY,
  `tokenable_type` varchar(255) NOT NULL,
  `tokenable_id` bigint unsigned NOT NULL,
  `name` text NOT NULL,
  `token` varchar(64) NOT NULL,
  `abilities` text,
  `last_used_at` timestamp NULL DEFAULT NULL,
  `expires_at` timestamp NULL DEFAULT NULL,
  `created_at` timestamp NULL DEFAULT NULL,
  `updated_at` timestamp NULL DEFAULT NULL,
  UNIQUE KEY `personal_access_tokens_token_unique` (`token`),
  KEY `personal_access_tokens_expires_at_index` (`expires_at`),
  KEY `personal_access_tokens_tokenable_type_tokenable_id_index` (`tokenable_type`,`tokenable_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

### 7.7 Laravel migration 代码范例（怎么改 schema）

实际工作中**不要手写 SQL**——所有表结构变更都通过新增一个 migration 文件来做，这样 Laravel 才能追踪 schema 版本。范例：

```bash
# 1) 用 artisan 生成 migration 文件（自动带时间戳前缀）
cd tycdn-backend
php artisan make:migration add_phone_to_users_table --table=users
```

生成的文件大致长这样（位于 `database/migrations/2026_05_20_XXXXXX_add_phone_to_users_table.php`）：

```php
<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up(): void
    {
        Schema::table('users', function (Blueprint $table) {
            $table->string('phone', 32)->nullable()->after('email');
            $table->index('phone');
        });
    }

    public function down(): void
    {
        Schema::table('users', function (Blueprint $table) {
            $table->dropIndex(['phone']);
            $table->dropColumn('phone');
        });
    }
};
```

```bash
# 2) 跑迁移（开发）
php artisan migrate

# 3) 跑迁移（生产）
php artisan migrate --force            # --force 跳过 production 环境的交互确认
```

**常用 Blueprint 方法速查**：

| 方法 | 等效 SQL | 说明 |
|---|---|---|
| `$table->id()` | `bigint unsigned PK AI` | 主键 |
| `$table->string('col', 100)` | `varchar(100)` | 默认 NOT NULL |
| `$table->text('col')` | `text` | 长文本 |
| `$table->integer('col')` | `int` | — |
| `$table->unsignedBigInteger('col')` | `bigint unsigned` | 用于外键列 |
| `$table->decimal('col', 18, 6)` | `decimal(18,6)` | 精确小数 |
| `$table->boolean('col')` | `tinyint(1)` | — |
| `$table->json('col')` | `json` | — |
| `$table->timestamp('col')` | `timestamp` | — |
| `$table->timestamps()` | `created_at` + `updated_at` | Laravel 自动维护 |
| `->nullable()` | `NULL` | — |
| `->default('val')` | `DEFAULT 'val'` | — |
| `->after('other_col')` | `AFTER other_col` | 物理位置 |
| `->unique()` | `UNIQUE INDEX` | — |
| `->index()` | `INDEX` | — |
| `->change()` | `ALTER COLUMN` | 改列类型 |

**外键专用**：

```php
$table->foreign('user_id')
    ->references('id')
    ->on('users')
    ->nullOnDelete();        // ON DELETE SET NULL
    // ->cascadeOnDelete()   // ON DELETE CASCADE
    // ->restrictOnDelete()  // ON DELETE RESTRICT
```

完整文档：<https://laravel.com/docs/13.x/migrations>。

---

## 8. 生产部署

> 真实凭据值在 [`上线前运维.md`](上线前运维.md) §1 —— **那份文档不能外传**。本章把流程、配置对照、隐藏坑讲清楚，但敏感值用 `<占位>` 表示。

### 8.1 部署总览（16 步清单）

| # | 步骤 | 详见 |
|---|---|---|
| 1 | 轮换所有弱密码与暴露凭据（8 个 .env 字段） | §8.8、[`上线前运维.md §1`](上线前运维.md) |
| 2 | 环境变量切生产值（`APP_ENV` / `APP_DEBUG` / `APP_URL` / `LOG_LEVEL`） | §8.2 |
| 3 | 数据库：创建非 root 专用账号 + `migrate --force` | §8.3 |
| 4 | 会话与认证（`SESSION_SECURE_COOKIE` / `SANCTUM_STATEFUL_DOMAINS` / `CORS_ALLOWED_ORIGINS` 等） | §8.2 |
| 5 | 缓存与队列推荐切到 Redis | §8.2 |
| 6 | 邮件指向生产 SMTP 并发测试 | §8.2 |
| 7 | reCAPTCHA 密钥（后端 + 前端两处） | §8.2 |
| 8 | CDNfly 出站打开 + 凭证有效 + 独立加密密钥 | §8.4 命令序列 |
| 9 | EPUSDT 通讯地址改生产 + 端到端测一笔支付 | §8.2 |
| 10 | Docker Compose 起服务 + migrate --force | §8.4 |
| 11 | 宿主机 Nginx 反代 + SSL | §8.7 |
| 12 | 替换品牌资产（Logo / Favicon / 邮件模板） | [`上线前运维.md §7`](上线前运维.md) |
| 13 | 安全加固（DB/Redis 不暴露公网、`.env` 600、`config:cache`、CSP、KMS） | [`上线前运维.md §8`](上线前运维.md) |
| 14 | 监控、日志、备份（异常报警、慢查询、健康检查、定时备份） | §8.6、[`上线前运维.md §9 §10`](上线前运维.md) |
| 15 | 前端构建（`npm ci && npx vite build`） | [`上线前运维.md §11`](上线前运维.md) |
| 16 | 端到端验证（注册→验证→登录→购买→支付→回调→开通） | §8.10 |

### 8.2 关键 .env 字段对照表

| 字段 | 本地默认 | 生产必填值 | 不改的后果 |
|---|---|---|---|
| `APP_ENV` | `local` | `production` | 错误页暴露代码/堆栈 |
| `APP_DEBUG` | `false`（已是安全值） | `false` | 同上 |
| `APP_URL` | `http://localhost:9000` | `https://api.你的域名.com` | 邮件链接 / 重定向是错的 |
| `APP_KEY` | 开发值 | `php artisan key:generate` 新生成 | 加密密钥被人拿到 |
| `DB_CONNECTION` | `sqlite` | `mysql` | 单文件 DB 撑不住 |
| `DB_HOST` / `DB_DATABASE` / `DB_USERNAME` / `DB_PASSWORD` | sqlite 没填 | 全部生产值 | 连不上库 |
| `SESSION_SECURE_COOKIE` | `false` | `true` | HTTP 拦截 cookie |
| `SESSION_DOMAIN` | 空 | `.你的域名.com` | 跨子域 SSO 失败 |
| `SANCTUM_STATEFUL_DOMAINS` | 一堆 localhost | 仅生产域名 | CSRF 失败 / SPA 不能登录 |
| `FRONTEND_URL` | `http://localhost:5173` | `https://portal.你的域名.com` | 邮件验证链接是错的 |
| `CORS_ALLOWED_ORIGINS` | localhost | `https://portal.你的域名.com` | 跨域被拦 |
| `CACHE_STORE` | `database` | `redis`（推荐） | 性能差但能用 |
| `QUEUE_CONNECTION` | `database` | `redis`（推荐） | 同上 |
| `MAIL_MAILER` | `log` | `smtp` | 用户收不到邮件 |
| `MAIL_HOST` / `MAIL_USERNAME` / `MAIL_PASSWORD` / `MAIL_FROM_ADDRESS` | 占位 | 生产 SMTP | 同上 |
| `RECAPTCHA_SITE_KEY` / `RECAPTCHA_SECRET_KEY` / `VITE_RECAPTCHA_SITE_KEY` | 空 | Google 申请的密钥 | 防刷不生效 |
| `EPUSDT_BASE_URL` / `EPUSDT_API_TOKEN` / `EPUSDT_NOTIFY_URL` / `EPUSDT_REDIRECT_URL` | `example.com` 占位 | 生产地址 | 支付不可用 |
| `EPUSDT_QUERY_ORDER_PATH` | 空 | EPUSDT 实例提供的查单路径 | 漏回调无法自动对账 |
| `CDNFLY_OUTBOUND_ENABLED` | `false` | `true` | 真实 CDN 不会被调用 |
| `CDNFLY_BASE_URL` / `CDNFLY_AUTH_TOKEN` / `CDNFLY_ADMIN_API_KEY` / `CDNFLY_ADMIN_API_SECRET` | 占位或乱码 | 从 CDNfly 面板取的真值 | CDN 集成失效 |
| `CDNFLY_ENCRYPTION_KEY` | 空 | `php artisan cdnfly:generate-key` | 凭证与 APP_KEY 不解耦 |
| `ADMIN_EMAIL` / `ADMIN_PASSWORD` | 默认或弱 | 上线后改强密码并删除该 env | 默认管理员被爆破 |

### 8.3 MySQL 怎么配（傻瓜手册）

> **一句话方案**：本地开发用**本机装的 MySQL**（127.0.0.1:3306），生产用项目自带的 **Docker MySQL**。
> 本节按这个方案展开。如果你将来想换云厂商的托管 MySQL（RDS），看 §8.3.6。
>
> ℹ️ 注意：[`tycdn-backend/.env.example:37`](tycdn-backend/.env.example) 的默认值是 `DB_CONNECTION=sqlite`（Laravel 入门姿势），但本项目实际的 `.env` 已经切到 mysql，**接手人本机也必须装 MySQL** 才能跑起来。

#### 8.3.1 整张图：MySQL 在哪、谁连它

```
┌─────────────────────────────────────────────────────────────┐
│ 你的笔记本（开发）                                            │
│                                                              │
│   ┌──────────────┐          ┌────────────────────┐          │
│   │ Laravel       │ ──────→ │ 本机 MySQL          │          │
│   │ (artisan     │          │ 127.0.0.1:3306     │          │
│   │  serve)      │          │ DB: tycdn_portal   │          │
│   └──────────────┘          └────────────────────┘          │
│                                                              │
│   .env 里 DB_HOST=127.0.0.1                                  │
│   推荐用 XAMPP / Laragon 一键装 MySQL                         │
└─────────────────────────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────┐
│ 生产服务器（你买的云主机，Linux）                              │
│                                                              │
│   ┌─────────── Docker（你只装这一个东西） ───────────┐      │
│   │                                                  │      │
│   │   ┌─────────────┐    ┌──────────────┐           │      │
│   │   │ app 容器     │ ──→│ mysql 容器    │           │      │
│   │   │ (Laravel)   │    │ (MySQL 8.0)  │           │      │
│   │   │             │    │              │           │      │
│   │   │ .env 里写：  │    │ 数据存在     │           │      │
│   │   │ DB_HOST=    │    │ Docker 卷    │           │      │
│   │   │   mysql     │    │ mysql-data   │           │      │
│   │   └─────────────┘    └──────────────┘           │      │
│   │                                                  │      │
│   │   它俩通过 Docker 内网通信，外部访问不到 MySQL   │      │
│   └──────────────────────────────────────────────────┘      │
│                                                              │
│ ✅ 你不需要 apt install mysql，docker compose 起来就有了    │
└─────────────────────────────────────────────────────────────┘
```

**关键认知**：

- **本地**：MySQL 装在你笔记本本机，Laravel 走 `127.0.0.1:3306` 连过去
- **生产**：MySQL 在 Docker 容器里跑，**不在服务器宿主机上跑**——你**不需要** `apt install mysql-server`
- **`DB_HOST=mysql` 不是打错**——这是 Docker 给容器起的"主机名"，只在 Docker 网络内有效（仅生产场景）
- **生产数据存在 Docker 卷** `mysql-data` 里，`docker volume rm` 就**毁了**

#### 8.3.2 本地开发：装 MySQL + 建库 + 跑 migrate

**第 1 步：装 MySQL**

Windows 推荐两种（任选一种，本机已有 MySQL 跳到第 2 步）：

- **XAMPP**：<https://www.apachefriends.org/> —— 下载 → 一键安装 → 启动 MySQL 模块（默认账号 root，无密码）
- **Laragon**：<https://laragon.org/download/> —— 下载 → 安装 → 启动 → 自带 MySQL 8.0（账号 root，无密码）

也可以直接装 MySQL 8.0 Community：<https://dev.mysql.com/downloads/installer/>。

启动后用 `mysql -uroot` 确认能连进去。

**第 2 步：建库**

进 MySQL 客户端：

```sql
CREATE DATABASE tycdn_portal
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;
```

> ⚠️ **必须用 utf8mb4**，否则中文 / emoji 乱码。

**第 3 步：配 .env**

```bash
cd tycdn-backend
cp .env.example .env
```

打开 `.env`，把 DB_* 这几行**取消注释并改成**：

```env
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=tycdn_portal
DB_USERNAME=root
DB_PASSWORD=               # XAMPP/Laragon 默认 root 无密码就留空；自装 MySQL 时填你设的密码
```

**第 4 步：跑 migrate + seed**

```bash
php artisan key:generate     # 生成 APP_KEY 写进 .env
php artisan migrate          # 在 tycdn_portal 库里建出 20 张表
php artisan db:seed          # 用 .env 里的 ADMIN_* 创建默认管理员
```

**第 5 步：验证**

```bash
mysql -uroot tycdn_portal -e "SHOW TABLES;"
# 应该列出 users / orders / products / payment_transactions / service_instances 等表
```

✅ 通过这一步本地就跑起来了。

> 💡 **图形化看数据**：用 DBeaver / Navicat / TablePlus，连 `127.0.0.1:3306`，用户名 root，密码同 `.env` 里的 `DB_PASSWORD`。
>
> ⚠️ 当前仓库 `.env` 里 `DB_PASSWORD=<已移除-见私密密码本>` 是**已知的弱密码**，本机自己玩可以保留，**绝对不要带到生产**（见 §8.8）。

#### 8.3.3 生产部署：5 步起服务

**前提**：你有一台 Linux 服务器（云主机随便买一个），并且：
- 已经 SSH 登上去
- 已经装了 Docker（没装就跑 `curl -fsSL https://get.docker.com | sh`）

**第 1 步：把项目代码复制到服务器**

把整个 `codex/1/` 目录复制到服务器上，假设放在 `/opt/tycdn/`：

```bash
# 你笔记本上执行
scp -r C:/Users/40606/Desktop/codex/1 root@你的服务器IP:/opt/tycdn
```

**第 2 步：在服务器上准备 `.env`**

```bash
# SSH 到服务器后执行
cd /opt/tycdn/tycdn-backend
cp .env.example .env
nano .env                              # 用 nano 或 vi 改 .env
```

**只看 MySQL 相关的 5 行**，按下面这样改：

```env
# 原本是：
DB_CONNECTION=sqlite
# DB_HOST=127.0.0.1
# DB_PORT=3306
# DB_DATABASE=laravel
# DB_USERNAME=root
# DB_PASSWORD=

# 改成（注意把 # 去掉，密码自己生成）：
DB_CONNECTION=mysql
DB_HOST=mysql
DB_PORT=3306
DB_DATABASE=tycdn_portal
DB_USERNAME=root
DB_PASSWORD=你自己生成的32位以上随机强密码
```

> ⚠️ **密码用什么生成？** 在服务器上跑 `openssl rand -base64 32` 拿一个 32 字节随机串。**记下来**，丢了就 GG（数据卷里的数据用这个密码加密锁着）。
>
> ⚠️ **为什么 `DB_USERNAME=root`？** 因为 [`docker-compose.yml:84`](tycdn-backend/docker-compose.yml) 里写的是 `MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}`，只创建了 root 账号。本项目目前**没单独建专用账号**——如果你要更安全，看 §8.3.5。

还有一行必须改的（不然 APP_KEY 没生成会报错）：

```bash
php artisan key:generate         # 自动写进 .env 的 APP_KEY=
```

**第 3 步：启动所有容器**

```bash
cd /opt/tycdn/tycdn-backend
docker compose up -d --build
```

`-d` 是后台运行，`--build` 是首次需要构建镜像。等 2-5 分钟，看到所有容器都是 `Up`：

```bash
docker compose ps
# 应该看到 app / queue / scheduler / nginx / mysql / redis / epusdt 全部 Up
# mysql 那行右边应该带 (healthy)
```

**第 4 步：让 Laravel 建表**

```bash
docker compose exec app php artisan migrate --force
docker compose exec app php artisan db:seed --force      # 首次部署，创建默认管理员
docker compose exec app php artisan config:cache         # 缓存配置
```

**第 5 步：验证 MySQL 通了**

```bash
# 验证 1：Laravel 能数到用户数
docker compose exec app php artisan tinker --execute="echo App\Models\User::count();"
# 应该输出一个数字（至少是 1，因为 seeder 建了默认管理员）

# 验证 2：直接连进 MySQL 看表
docker compose exec mysql mysql -uroot -p"你刚才设的密码" tycdn_portal -e "SHOW TABLES;"
# 应该列出 users / orders / products / ... 等表

# 验证 3：看是不是 utf8mb4
docker compose exec mysql mysql -uroot -p"你刚才设的密码" -e "SHOW VARIABLES LIKE 'character_set_database';"
# 应该是 utf8mb4
```

✅ 三个验证都过，MySQL 就配好了。

#### 8.3.4 .env 字段精确对照

| 字段 | 本地（不用动） | 生产（必须改） | 这玩意儿是啥 |
|---|---|---|---|
| `DB_CONNECTION` | `sqlite` | `mysql` | 用哪种数据库 |
| `DB_HOST` | （注释掉） | `mysql` | MySQL 的主机名。Docker 部署里**就是 `mysql` 这 5 个字符**，因为它是容器服务名 |
| `DB_PORT` | （注释掉） | `3306` | MySQL 端口，标准就是 3306 |
| `DB_DATABASE` | （注释掉） | `tycdn_portal` | 数据库名。`docker-compose.yml` 会自动建这个库 |
| `DB_USERNAME` | （注释掉） | `root` | 当前是 root（见 §8.3.3 第 2 步备注） |
| `DB_PASSWORD` | （注释掉） | `<32 位强密码>` | root 的密码，同时也是 `docker-compose.yml` 创建 MySQL 时传给 `MYSQL_ROOT_PASSWORD` 的值 |

#### 8.3.5 想用专用账号（不用 root）的进阶做法

默认配置里 Laravel 用 root 连 MySQL，能跑但不算最佳实践。要换成专用账号：

**第 1 步**：让 docker-compose 起来后，进 mysql 容器创建专用账号：

```bash
docker compose exec mysql mysql -uroot -p"$DB_PASSWORD" <<'SQL'
CREATE USER 'tycdn_app'@'%' IDENTIFIED BY '另一个32位强密码';
GRANT SELECT, INSERT, UPDATE, DELETE, CREATE, ALTER, INDEX, DROP, REFERENCES
  ON tycdn_portal.* TO 'tycdn_app'@'%';
FLUSH PRIVILEGES;
SQL
```

**第 2 步**：改 `.env`：

```env
DB_USERNAME=tycdn_app
DB_PASSWORD=另一个32位强密码         # 注意这是 tycdn_app 的密码
                                     # MYSQL_ROOT_PASSWORD 仍由 docker-compose.yml 的 ${DB_PASSWORD} 决定
                                     # —— 所以严格做的话，docker-compose.yml 要拆成两个变量
```

> 这个分离做得彻底要改 `docker-compose.yml`：把 `MYSQL_ROOT_PASSWORD` 改成读独立变量（比如 `${DB_ROOT_PASSWORD}`），别再复用 `DB_PASSWORD`。不改也能跑，只是 root 密码 = app 密码。

**第 3 步**：重启 app 容器让新配置生效：

```bash
docker compose restart app queue scheduler
```

#### 8.3.6 换成云厂商 RDS（阿里云 / AWS / 腾讯云等）

如果你不想让 MySQL 跑在自己服务器上：

**第 1 步**：在云控制台开一个 MySQL 8.0 实例，把数据库名建为 `tycdn_portal`，字符集选 `utf8mb4` / `utf8mb4_unicode_ci`，拿到：
- 连接地址（如 `rm-xxx.mysql.rds.aliyuncs.com`）
- 端口（一般 3306）
- 账号 / 密码

**第 2 步**：把云 MySQL 的"IP 白名单"加上你的服务器公网 IP。

**第 3 步**：改 `.env`：

```env
DB_HOST=rm-xxx.mysql.rds.aliyuncs.com    # ← 改成 RDS 地址
DB_PORT=3306
DB_DATABASE=tycdn_portal
DB_USERNAME=云上账号
DB_PASSWORD=云上密码
```

**第 4 步**：把 docker-compose 的 `mysql` 服务**注释掉或删掉**（用不上了），并把 `app` / `queue` / `scheduler` 的 `depends_on: mysql` 也删掉。

**第 5 步**：照常 `docker compose up -d`，然后 `docker compose exec app php artisan migrate --force`。

#### 8.3.7 常见疑问 FAQ

**Q1：我要不要自己 `apt install mysql-server`？**  
不要。用 Docker 方案的话，MySQL 跑在容器里，宿主机不装 MySQL。

**Q2：`DB_HOST=mysql` 这是啥地址？我 ping 不通**  
`mysql` 是 Docker 容器的内部名字（来自 [`docker-compose.yml`](tycdn-backend/docker-compose.yml) 里的 `mysql:` 服务名），**只在 Docker 网络内有效**。你宿主机 ping 不通是正常的。Laravel 容器和 MySQL 容器在同一个 Docker 网络（`tycdn`），所以它们之间能用 `mysql` 这个名字互相找到。

**Q3：我怎么登 MySQL 看数据？**

```bash
# 命令行
docker compose exec mysql mysql -uroot -p"$DB_PASSWORD" tycdn_portal

# 图形化（用 Navicat / DBeaver）：
# 先临时把 mysql 端口透出：docker-compose.yml 里 ports 写 "127.0.0.1:3307:3306"
# 然后客户端连 127.0.0.1:3307，用户名 root，密码同 .env 里的 DB_PASSWORD
# 生产环境用完记得改回不透出
```

**Q4：迁移失败 / 报错怎么办？**  
最常见三种：
- `SQLSTATE[HY000] [2002] Connection refused` —— MySQL 还没起好，等 30 秒重试，或 `docker compose logs mysql` 看日志
- `Access denied for user 'root'` —— `.env` 的 `DB_PASSWORD` 和 docker-compose 启动 mysql 时用的不一致；改完密码要 `docker compose down -v mysql && docker compose up -d` 才会重新初始化（**注意：`-v` 会清数据卷**，首次部署没数据不要紧）
- `Unknown database 'tycdn_portal'` —— `.env` 的 `DB_DATABASE` 和 docker-compose 建库的名字不一致

**Q5：数据怎么备份？**

```bash
# 每天备份一次（放进 crontab）
docker compose exec -T mysql mysqldump \
  -uroot -p"$DB_PASSWORD" \
  --single-transaction --quick \
  tycdn_portal | gzip > /backup/tycdn-$(date +%F).sql.gz
```

**Q6：上线后想升级 MySQL 8.0 → 8.1，会丢数据吗？**  
不会，但要按部就班：先 `mysqldump` 完整备份 → 改 docker-compose 里的镜像版本 → `docker compose up -d mysql` → MySQL 容器会自动升级 data 目录。**但跨大版本（8 → 9）务必先在测试环境验证一遍**。

**Q7：那 `php artisan migrate` 到底干了啥？**  
它读 `database/migrations/` 下 20 个文件，按时间顺序在 MySQL 里建表 / 改表（建表 SQL 见 §7.5）。它还会在 MySQL 里维护一张内部表 `migrations`，记录"哪些 migration 已经跑过"，所以重复跑 `migrate` 不会重复建表。

**Q8：字符集排查（上线后看到 `?` 或乱码）**

```sql
SHOW VARIABLES LIKE 'character_set_%';   -- 全部应该是 utf8mb4
SHOW VARIABLES LIKE 'collation_%';       -- 应该是 utf8mb4_unicode_ci
```

如果不是，进 mysql 容器：

```bash
docker compose exec mysql mysql -uroot -p"$DB_PASSWORD" -e "
  ALTER DATABASE tycdn_portal CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
"
```

> [`docker-compose.yml:85-86`](tycdn-backend/docker-compose.yml) 里已经设了 `MYSQL_CHARSET: utf8mb4` 和 `MYSQL_COLLATION: utf8mb4_unicode_ci`，正常不会有乱码。乱码一般是后来 ALTER 过表或导入了非 utf8mb4 的数据。

### 8.4 Docker Compose 服务清单

[`tycdn-backend/docker-compose.yml`](tycdn-backend/docker-compose.yml) 编排了 8 个服务：

| 服务 | 镜像 / 来源 | 作用 | 暴露端口 |
|---|---|---|---|
| `app` | 本地构建 `docker/app.Dockerfile` | Laravel PHP-FPM 主进程 | 仅内网 |
| `queue` | 同 app | 跑 `queue:work` 处理异步任务 | — |
| `scheduler` | 同 app | 每 60 秒跑 `schedule:run` 触发定时任务 | — |
| `nginx` | `nginx:alpine` | 反向代理 + 静态文件 | **80**（API）、**8080**（门户） |
| `portal-build` | 本地构建 `docker/portal.Dockerfile` | 一次性构建 Vue 门户并拷到共享卷 | — |
| `mysql` | `mysql:8.0` | 数据库 | **3306**（默认透出，⚠️ 生产要关，见 §8.5） |
| `redis` | `redis:7-alpine` | 缓存 / 队列 / 会话 | 仅内网 |
| `epusdt` | `gmwallet/epusdt:latest`（官方镜像） | USDT 收款服务 | **8001**（宿主） → 8000（容器内）⚠️ 管理端口不能暴露公网 |

**首次部署命令序列**：

```bash
cd tycdn-backend

# 1) 准备配置
cp .env.example .env
nano .env                                            # 按 §8.2 改所有值
php artisan key:generate                             # 生成 APP_KEY

# 2) 构建并启动
docker compose up -d --build

# 3) 等 mysql 健康检查通过后跑迁移
docker compose exec app php artisan migrate --force
docker compose exec app php artisan db:seed --force  # 仅首次

# 4)（推荐）CDNfly 凭证独立加密
docker compose exec app php artisan cdnfly:generate-key
docker compose exec app php artisan cdnfly:rekey

# 5) 缓存配置 / 路由
docker compose exec app php artisan config:cache
docker compose exec app php artisan route:cache

# 6) 验证
docker compose ps                                    # 所有服务应为 Up / healthy
docker compose exec app php artisan about
```

### 8.5 docker-compose.yml 里的 3 个隐藏坑 ⚠️

**坑 1：MySQL root 密码默认值**  
[`docker-compose.yml:84`](tycdn-backend/docker-compose.yml) 用 `${DB_PASSWORD:-secret}` 作为 `MYSQL_ROOT_PASSWORD`。如果 `.env` 没设 `DB_PASSWORD`，root 密码就是字符串 `secret`。上线必须确保 `.env` 设了强密码。

**坑 2：MySQL 3306 默认透出宿主机**  
`ports: "${DB_FORWARD_PORT:-3306}:3306"` 把容器 3306 映射到宿主机。生产环境**应注释掉**，或改为只绑定本地：`127.0.0.1:3307:3306`。

**坑 3：EPUSDT 8001 默认透出**  
`epusdt` 服务的 8001 是管理面板端口，**绝对不能暴露公网**。通过宿主机 Nginx 加 IP 白名单或 Basic Auth 限制。

### 8.6 数据卷与备份

| Volume | 内容 | 备份重要性 |
|---|---|---|
| `mysql-data` | 业务数据全部在这 | ⭐⭐⭐ 必须定时备份 |
| `app-storage` | Laravel 上传文件、日志、缓存 | ⭐⭐ 日志可丢，上传文件要备 |
| `redis-data` | 会话 / 队列 / 缓存 | ⭐ 可丢失（用户需重新登录） |
| `epusdt-data` | EPUSDT 服务自身数据（订单签名等） | ⭐⭐⭐ 必须备份 |
| `portal-dist` | 前端构建产物，可重建 | — |

**备份脚本范例**（cron 每日凌晨 2 点）：

```bash
docker compose exec -T mysql mysqldump \
  -u root -p"$DB_PASSWORD" \
  --single-transaction --quick \
  tycdn_portal | gzip > /backup/db-$(date +%F).sql.gz
```

### 8.7 宿主机 Nginx + SSL（终止层）

Docker 内部 Nginx 监听 80（API）和 8080（门户），但**没有 SSL**。宿主机需要再起一个 Nginx 做 SSL 终止 + 反向代理：

```nginx
# /etc/nginx/sites-enabled/tycdn.conf

server {
    listen 443 ssl http2;
    server_name api.你的域名.com;
    ssl_certificate     /etc/letsencrypt/live/api.你的域名.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/api.你的域名.com/privkey.pem;

    location / {
        proxy_pass http://127.0.0.1:80;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

server {
    listen 443 ssl http2;
    server_name portal.你的域名.com;
    ssl_certificate     /etc/letsencrypt/live/portal.你的域名.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/portal.你的域名.com/privkey.pem;

    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header Host              $host;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# HTTP → HTTPS 跳转
server {
    listen 80;
    server_name api.你的域名.com portal.你的域名.com;
    return 301 https://$host$request_uri;
}
```

SSL 证书：`certbot --nginx -d api.你的域名.com -d portal.你的域名.com`。

### 8.8 凭据轮换清单（最高优先级）⚠️

当前 `.env` 至少 8 个字段是弱密码、乱码或开发值，**全部必须换**。真实问题对照见 [`上线前运维.md` §1](上线前运维.md)。

| 字段 | 问题类型 | 操作 |
|---|---|---|
| `DB_PASSWORD` | 弱密码 | 32+ 位随机密码，同步 MySQL 账号 |
| `MAIL_PASSWORD` | 弱密码 | 邮件服务商控制台重置 SMTP 密码 |
| `EPUSDT_API_TOKEN` | 弱密码 | EPUSDT 服务重新生成 Token |
| `ADMIN_PASSWORD` | 弱密码 | 强密码；首次 seed 后建议删除该字段 |
| `CDNFLY_AUTH_TOKEN` | UTF-8 损坏的乱码 | 从 CDNfly 面板获取正确 Bearer Token |
| `CDNFLY_ADMIN_API_KEY` | 已暴露 | CDNfly 面板重新生成 |
| `CDNFLY_ADMIN_API_SECRET` | 已暴露 | 同上 |
| `APP_KEY` | 开发值 | `php artisan key:generate` |

**额外建议**：把这些凭据从 `.env` 迁到 KMS（Docker Secrets / HashiCorp Vault / 云 KMS），见 [`上线前运维.md §8.2`](上线前运维.md)。

### 8.9 EPUSDT 服务配置（密钥与收款地址）

EPUSDT 是个**独立的开源 USDT 收单网关**（来自 [github.com/GMWalletApp/epusdt](https://github.com/GMWalletApp/epusdt)，GPLv3，Go 写的）。它与 Laravel **完全解耦**——Laravel 只是个调用方，**真正的钱包地址、商户密钥都配在 EPUSDT 服务自己那边**。

> 💡 **认知前提**：你不是在 Laravel 的 `.env` 里设钱包私钥（私钥永远不在代码里）。流程是：① 在 EPUSDT 服务里加好你的 USDT 钱包地址 → ② EPUSDT 给你分配商户号 + API Token → ③ 把这两个值填进 Laravel 的 `.env`。

#### 8.9.1 EPUSDT 部署方式

> ℹ️ **2026-05 改造记录**：原本 docker-compose 挂载本地 `epusdt-binary` 二进制（37 MB）跑在 `alpine:3.19` 容器里。现已改为**官方镜像 `gmwallet/epusdt:latest`**，本地不再需要任何 EPUSDT 文件。接手人 `docker compose up` 时镜像会自动从 Docker Hub 拉取。

| 文件 / 服务 | 现状 |
|---|---|
| [`tycdn-backend/docker-compose.yml`](tycdn-backend/docker-compose.yml) `epusdt` 服务 | 用 `gmwallet/epusdt:latest` 官方镜像，宿主 8001 → 容器 8000 |
| `epusdt-binary`（已废弃） | 不在 git 仓库里。如果你想自托管二进制版本，从 [github.com/GMWalletApp/epusdt/releases](https://github.com/GMWalletApp/epusdt/releases) 下载 |
| `epusdt-data` Docker 卷 | 服务的 SQLite 运行时数据库（订单 / 钱包地址 / 商户配置），自动持久化在 `/app/runtime` |

#### 8.9.2 EPUSDT 服务自己的 .env（与 Laravel 的 .env 不同）

EPUSDT 服务**自己**也有一份配置（参考 [`epusdt-master.zip`](epusdt-master.zip) 里的 `src/.env.example`），关键字段：

```env
http_listen=:8000              # EPUSDT 自己监听容器内 8000；docker-compose 把它映射到宿主 8001
db_type=sqlite                 # 用 SQLite 就够，低并发场景零依赖
runtime_sqlite_filename=epusdt-runtime.db
log_level=info
order_expiration_time=10       # 订单过期分钟数（要和 Laravel orders.expire_at 一致）

# 首次启动跑 Web 安装向导，设管理员账号
install=true
```

⚠️ **不要把这份 .env 和 Laravel 的 .env 混了**——它们是两个不同的服务，各自有一份 .env。EPUSDT 官方镜像启动时会自动跑 Web 安装向导（`install=true`），向导跑完后配置会写进容器内 `/app/runtime/` 目录（由 `epusdt-data` 卷持久化）。你**不需要手动准备**这份 .env。

#### 8.9.3 启动 + 初始化 EPUSDT 服务（5 步）

**第 1 步：起容器**

按 §8.4 跑 `docker compose up -d` 时，`epusdt` 容器会自动从 Docker Hub 拉 `gmwallet/epusdt:latest` 镜像并启动，监听宿主机的 **8001 端口**（[`docker-compose.yml`](tycdn-backend/docker-compose.yml) 里 `${EPUSDT_PORT:-8001}:8000`，宿主 8001 → 容器 8000）。

```bash
docker compose ps epusdt
# 应该看到 Up
docker compose logs epusdt | head -20
# 第一次启动会看到 "install wizard" 之类的提示
```

**第 2 步：打开 Web 安装向导**

浏览器访问：`http://服务器内网IP:8001`（**不要从公网访问**——见下文安全说明）。

或者通过 SSH 隧道：

```bash
# 在你笔记本上跑：把服务器 8001 转发到本机 8001
ssh -L 8001:127.0.0.1:8001 root@你的服务器IP
# 然后笔记本浏览器访问 http://localhost:8001
```

按向导填：
- **管理员账号 + 密码**（设强密码，将来登 EPUSDT 后台用）
- **数据库类型**：开发/小流量选 SQLite（默认），高并发再考虑 MySQL
- 安装完成后向导会自动把 `install=true` 改成 `false`

**第 3 步：在 EPUSDT 后台加你的 USDT 钱包地址**

登 EPUSDT 后台 → 钱包管理（或 "Wallet" 菜单）→ 新增钱包：

| 字段 | 怎么填 |
|---|---|
| 网络 | `TRC20`（Laravel 默认是 TRC20，见 [`config/services.php:52`](tycdn-backend/config/services.php) `default_network`） |
| 代币 | `USDT` |
| 钱包地址 | 你自己的 TRC20 USDT 收款地址（从 TronLink / imToken / 火币 / 币安等钱包导出） |
| 状态 | 启用 |

> ⚠️ **私钥永远不要填进 EPUSDT**——EPUSDT 只需要**地址**（公钥），用于监听入账。私钥留在你自己的钱包 App 里。
>
> 💡 **轮询机制**：可以加多个钱包地址，EPUSDT 会自动轮询，提高并发处理能力。建议至少加 3-5 个。

**第 4 步：在 EPUSDT 后台创建商户（给 Laravel 用）**

登 EPUSDT 后台 → 商户管理（或 "Merchant"/"API"）→ 新增商户：

| 字段 | 怎么填 |
|---|---|
| 商户名称 | 任意（如 "TyCDN-Production"） |
| 回调白名单 | 你的 Laravel 后端域名（如 `https://api.你的域名.com`） |

提交后 EPUSDT 会给你：
- **PID**（商户号，纯数字，类似 `1000`）
- **API Token**（签名密钥，随机字符串）

**把这两个值记下来**——下一步要填进 Laravel 的 .env。

**第 5 步：填回 Laravel .env**

```env
# Laravel /opt/tycdn/tycdn-backend/.env
EPUSDT_BASE_URL=http://epusdt:8001                                     # Docker 网络内访问 EPUSDT
EPUSDT_PID=1000                                                        # 第 4 步拿到的商户号
EPUSDT_API_TOKEN=第4步拿到的随机字符串                                   # ⚠️ 强密钥，不能弱
EPUSDT_CREATE_ORDER_PATH=/payments/gmpay/v1/order/create-transaction   # GMPay 接口路径，保持默认
EPUSDT_QUERY_ORDER_PATH=/payments/gmpay/v1/order/query                 # 漏回调对账用（见 §6.2，路径以 EPUSDT 官方文档为准）
EPUSDT_NOTIFY_URL=https://api.你的域名.com/api/payments/epusdt/notify   # EPUSDT 回调你的地址
EPUSDT_REDIRECT_URL=https://portal.你的域名.com/console/billing/orders  # 用户付款后跳回门户
EPUSDT_DEFAULT_CURRENCY=USD                                            # 法币计价
EPUSDT_DEFAULT_TOKEN=usdt
EPUSDT_DEFAULT_NETWORK=TRON
EPUSDT_RATE_API_URL=https://cdn.jsdelivr.net/npm/@fawazahmed0/currency-api@latest/v1/currencies/usd.min.json
EPUSDT_RATE_JSON_PATH=usd.cny                                          # 法币 → USDT 汇率源
```

改完 `.env` 必须重启 Laravel 容器让配置生效：

```bash
docker compose restart app queue scheduler
docker compose exec app php artisan config:cache
```

#### 8.9.4 验证 EPUSDT 通了

**测试 1：Laravel 能拼出正确签名**

```bash
docker compose exec app php artisan tinker --execute='
  $svc = app(App\Services\EpusdtService::class);
  echo $svc->sign(["order_id" => "TEST123", "amount" => "1.00"]) . PHP_EOL;
'
# 应该输出 32 位 md5 字符串，不抛异常说明 EPUSDT_API_TOKEN 已注入
```

**测试 2：在 console 下单走完整支付链路**

1. 登录用户门户，选最便宜的套餐下单
2. 检查能否跳到 EPUSDT 收银台（页面会显示 USDT 收款地址 + 金额）
3. 用你自己的钱包（TronLink 等）按页面金额向收款地址转 USDT
4. EPUSDT 监听到入账后会自动 POST 到 `EPUSDT_NOTIFY_URL`，Laravel 把订单状态改为 `paid`
5. 在 Laravel 控制台看订单状态变为已支付

**测试 3：检查 EPUSDT 数据库有没有写入**

```bash
docker compose exec epusdt ls -la /app/runtime/
# 应该看到 epusdt-runtime.db 文件，且体积在增长

# 想直接看库里订单数据（高级）：
docker compose exec epusdt sqlite3 /app/runtime/epusdt-runtime.db "SELECT * FROM orders ORDER BY id DESC LIMIT 5;"
```

#### 8.9.5 EPUSDT 关键安全要求 ⚠️

1. **8001 端口绝对不能暴露公网**——管理面板被人拿到就能改钱包地址、把你的钱导走。生产部署必须：
   - 在宿主机 Nginx 上加 IP 白名单（只允许你自己运维 IP 访问）
   - 或者根本不开 8001 到公网，只通过 SSH 隧道访问
   - 见 §8.5 隐藏坑 3
2. **`EPUSDT_API_TOKEN` 是强签名密钥**——当前 `.env` 里是弱密码 `<已移除-见私密密码本>`，**必须换**成 EPUSDT 后台重新生成的强 token（见 §8.8）
3. **回调验签是支付安全的生命线**——`Api/EpusdtController::notify` 会用 `EPUSDT_API_TOKEN` 做 md5 签名校验，任何不带正确签名的回调都会被拒绝。不要禁用这个校验
4. **EPUSDT 运行时数据库要备份**——`epusdt-data` Docker 卷里的 `epusdt-runtime.db` 存订单、签名 / 商户配置，丢了就要重新接钱包、重发商户密钥。备份脚本见 §8.6
5. **钱包私钥永远不进服务器**——EPUSDT 只用地址（公钥），私钥留在你的钱包 App 里。如果钱包 App 在手机上，注意手机本身的安全

#### 8.9.6 EPUSDT 官方文档（深入阅读）

| 资源 | 链接 |
|---|---|
| 官方文档站 | <https://epusdt.com> |
| Docker 部署指南 | <https://epusdt.com/guide/installation/docker> |
| GMPay API 文档（Laravel 调用的就是这套接口） | <https://epusdt.com/zh/guide/integration/gmpay.html> |
| GitHub 仓库 | <https://github.com/GMWalletApp/epusdt> |
| Telegram 频道 | <https://t.me/epusdt>（升级公告、漏洞通知，建议关注） |

---

### 8.10 部署后烟雾测试 5 步

每一步过了再下一步：

1. **基础健康**：访问 `https://api.你的域名.com` 应返回 200 或 JSON（不是 502）；`docker compose exec app php artisan about` 输出正常
2. **DB 连通**：`docker compose exec app php artisan tinker --execute="echo App\Models\User::count();"` 返回数字
3. **邮件**：注册新用户 → 收到验证邮件 → 点击链接验证成功
4. **支付**：控制台下一个最小金额订单 → EPUSDT 收银台显示 → 完成支付 → 订单状态变 paid
5. **CDN 开通**：付款成功后，CDNfly 面板出现对应用户与套餐记录

---

## 9. 文档与代码索引

### 9.1 项目文档

| 你想知道什么 | 看哪里 |
|---|---|
| 完整待办清单（每天最新） | [`待办.md`](待办.md) |
| 上线前所有可勾选事项（含敏感真值，⚠️ 不能外传） | [`上线前运维.md`](上线前运维.md) |
| 安全审查报告 | [`审查报告.md`](审查报告.md) |
| CC 规则结构化表单方案 | [`docs/CC规则结构化表单方案.md`](docs/CC规则结构化表单方案.md) |
| CDNfly 各模块设计文档（PDF） | [`docs/`](docs/) 下 7 份 PDF（证书 / 节点组 / 网站 / 站点组 / 四层组 / 四层流 / 用户配置） |

### 9.2 代码入口（迷路时回到这里）

| 想读什么 | 看哪个文件 |
|---|---|
| API 路由总览（接手第一个要看的文件） | [`tycdn-backend/routes/api.php`](tycdn-backend/routes/api.php) |
| 数据库表结构 | [`tycdn-backend/database/migrations/`](tycdn-backend/database/migrations) |
| 后端配置文件 | [`tycdn-backend/config/`](tycdn-backend/config) |
| Docker 编排 | [`tycdn-backend/docker-compose.yml`](tycdn-backend/docker-compose.yml) |
| 前端路由 | [`frontend/src/router/`](frontend/src/router) |
| 前端 API 客户端 | [`frontend/src/api/`](frontend/src/api) |
| 前端 HTTP 拦截器（CSRF 自动重试） | [`frontend/src/api/http.js`](frontend/src/api/http.js) |
| redirect 安全校验 | [`frontend/src/config/runtime.js`](frontend/src/config/runtime.js) |
| 管理控制台 Vue 页面（Inertia） | [`tycdn-backend/resources/js/Pages/`](tycdn-backend/resources/js/Pages) |

### 9.3 常用 artisan 命令（在 `tycdn-backend/` 目录下运行）

```bash
php artisan migrate                 # 跑数据库迁移
php artisan db:seed                 # 跑 seeder（创建默认管理员）
php artisan tinker                  # 进 Laravel REPL（调试）
php artisan route:list              # 列出所有路由
php artisan payments:reconcile      # 手动跑一次支付对账
php artisan cdnfly:generate-key     # 生成 CDNfly 独立加密密钥
php artisan cdnfly:rekey            # 把已有用户凭证迁到独立密钥
php artisan cdnfly:rotate-keys      # 轮换 90 天以上未同步的 API Key
php artisan config:cache            # 生产环境缓存配置
php artisan config:clear            # 清缓存（修改 .env 后必做）
```
