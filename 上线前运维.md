# TyCDN 上线前运维清单

> 本文档列出所有代码层面已准备就绪、但需要在部署到生产环境前手动完成的运维事项。
> 每项完成后在 `[ ]` 中填入 `x` 标记为已完成。

---

## 1. 凭据轮换（最高优先级）

当前 `.env` 中多处**复用同一个弱密码**，必须在上线前全部替换为各自独立的强密码。

> 🔒 该密码原文曾以明文写在本文档与 `开发交接.md` 中，现已移除。
> **仅移除文本不等于安全**：它仍存在于本仓库的 git 历史里。请务必
> ① 先在各服务端轮换这些凭据，② 再重写 git 历史（`git filter-repo` 或重新初始化仓库）。
> 真实值只应保存在不进仓库的私密密码本中（见根目录 `.gitignore` 的 `交接文档.md`）。

| 变量 | 当前问题 | 操作 |
|------|----------|------|
| `DB_PASSWORD` | 弱密码 `<已移除-见私密密码本>` | 替换为 32+ 位随机密码，同步更新 MySQL 用户密码 |
| `MAIL_PASSWORD` | 弱密码 `<已移除-见私密密码本>` | 在腾讯云邮件控制台重置 SMTP 密码，更新此值 |
| `EPUSDT_API_TOKEN` | 弱密码 `<已移除-见私密密码本>` | 在 EPUSDT 服务配置中重新生成 API Token，更新此值 |
| `ADMIN_PASSWORD` | 弱密码 `<已移除-见私密密码本>` | 替换为强密码（仅用于首次 seed，之后可删除此变量） |
| `CDNFLY_AUTH_TOKEN` | 乱码值 `浣犵殑API瀵嗛挜`（UTF-8 编码损坏） | 从 CDNfly 面板获取正确的 Bearer Token，替换此值 |
| `CDNFLY_ADMIN_API_KEY` | 已暴露在本地 `.env` | 确认此密钥是否仍然有效；如已泄露，在 CDNfly 面板重新生成 |
| `CDNFLY_ADMIN_API_SECRET` | 已暴露在本地 `.env` | 同上，与 API_KEY 一起轮换 |
| `APP_KEY` | 当前值 `<已移除-见私密密码本>` | 生产环境应使用 `php artisan key:generate` 生成全新密钥 |

- [ ] 全部凭据已替换为独立强密码
- [ ] 旧密码已从所有备份和历史记录中清除

---

## 2. 环境变量切换

以下变量在本地开发中使用占位值，上线前必须改为生产值。

### 2.1 应用基础

```env
APP_ENV=production
APP_DEBUG=false
APP_URL=https://api.你的域名.com        # Laravel 后端公网地址
LOG_LEVEL=warning                        # 生产环境不需要 debug 级别
```

- [ ] `APP_ENV` 已改为 `production`
- [ ] `APP_DEBUG` 已确认为 `false`
- [ ] `APP_URL` 已改为生产域名
- [ ] `LOG_LEVEL` 已改为 `warning` 或 `error`

### 2.2 数据库

```env
DB_HOST=mysql                            # Docker 内部服务名，或生产 RDS 地址
DB_DATABASE=tycdn_portal                 # 确认数据库名
DB_USERNAME=tycdn_app                    # 不要用 root，创建专用数据库用户
DB_PASSWORD=（强密码）
```

- [ ] 已创建专用数据库用户（非 root），仅授予必要权限
- [ ] 已运行 `php artisan migrate` 完成数据库迁移

### 2.3 会话与认证

```env
SESSION_SECURE_COOKIE=true               # HTTPS 部署后必须开启
SESSION_DOMAIN=.你的域名.com             # 如果门户和控制台在同一父域下共享 session
SANCTUM_STATEFUL_DOMAINS=portal.你的域名.com,api.你的域名.com
FRONTEND_URL=https://portal.你的域名.com  # 门户公网地址
CORS_ALLOWED_ORIGINS=https://portal.你的域名.com
```

- [ ] `SESSION_SECURE_COOKIE=true`（HTTPS 部署后）
- [ ] `SANCTUM_STATEFUL_DOMAINS` 只包含生产域名，移除所有 localhost
- [ ] `SESSION_DOMAIN` 已根据域名架构配置
- [ ] `FRONTEND_URL` 已改为生产门户地址
- [ ] `CORS_ALLOWED_ORIGINS` 只包含生产域名

### 2.4 缓存与队列

```env
CACHE_STORE=redis                        # 生产环境建议 Redis
QUEUE_CONNECTION=redis                   # 生产环境建议 Redis
REDIS_HOST=redis                         # Docker 内部服务名
REDIS_PASSWORD=（如果 Redis 设置了密码）
```

- [ ] 已从 `database` 切换到 `redis`（如使用 Redis）
- [ ] Redis 已设置访问密码（如果暴露外网端口）

### 2.5 邮件

```env
MAIL_MAILER=smtp
MAIL_HOST=sg-smtp.qcloudmail.com
MAIL_PORT=465
MAIL_USERNAME=（生产发信地址）
MAIL_PASSWORD=（生产密码）
MAIL_FROM_ADDRESS=noreply@你的域名.com
MAIL_FROM_NAME=TyCDN
```

- [ ] 邮件配置指向生产 SMTP 服务
- [ ] 已发送测试邮件验证连通性
- [ ] `MAIL_FROM_ADDRESS` 已改为正式发信地址

---

## 3. reCAPTCHA 配置

找回密码页面已升级为 Google reCAPTCHA v2 Checkbox，需要配置密钥对。

1. 前往 https://www.google.com/recaptcha/admin 注册站点
2. 类型选择 **reCAPTCHA v2 — "I'm not a robot" Checkbox**
3. 域名填写你的门户域名（如 `portal.你的域名.com`）

```env
# Laravel .env
RECAPTCHA_SITE_KEY=6Le...你的site key
RECAPTCHA_SECRET_KEY=6Le...你的secret key

# 前端 frontend/.env（构建期变量）
VITE_RECAPTCHA_SITE_KEY=6Le...你的site key
```

- [ ] 已注册 reCAPTCHA 站点并获取密钥对
- [ ] 后端 `.env` 已配置 `RECAPTCHA_SITE_KEY` 和 `RECAPTCHA_SECRET_KEY`
- [ ] 前端 `frontend/.env` 已配置 `VITE_RECAPTCHA_SITE_KEY`
- [ ] 已在找回密码页面测试 reCAPTCHA 正常工作

---

## 4. CDNfly 集成

```env
CDNFLY_OUTBOUND_ENABLED=true             # 上线时开启 CDNfly 出站通讯
CDNFLY_BASE_URL=https://cdn666.com       # 必须 HTTPS（代码层强制校验）
CDNFLY_PROVISION_URL=https://cdn666.com/v1/user-packages
CDNFLY_AUTH_TOKEN=（从 CDNfly 获取的有效 Bearer Token）
CDNFLY_ADMIN_API_KEY=（从 CDNfly 获取的管理员 API Key）
CDNFLY_ADMIN_API_SECRET=（从 CDNfly 获取的管理员 API Secret）
```

- [ ] 已从 CDNfly 面板获取有效的 Admin API Key / Secret
- [ ] 已获取有效的 Auth Token（替换乱码值）
- [ ] `CDNFLY_OUTBOUND_ENABLED` 已改为 `true`
- [ ] `CDNFLY_BASE_URL` 使用 `https://`（代码层会拒绝 HTTP）
- [ ] 已测试：注册 → 邮箱验证 → CDNfly 用户自动创建 → API Key 同步
- [ ] 已测试：购买套餐 → 支付 → 自动开通（provision）流程

### 4.1 CDNfly 凭证独立加密（推荐）

将用户的 CDNfly API Key/Secret 加密与 APP_KEY 解耦，防止 APP_KEY 泄露连带暴露所有凭证。

```bash
# 1. 生成独立加密密钥（自动写入 .env）
php artisan cdnfly:generate-key

# 2. 将已有用户凭证从 APP_KEY 迁移到新密钥
php artisan cdnfly:rekey --dry-run   # 先预览
php artisan cdnfly:rekey             # 执行迁移
```

```env
# 生成后 .env 会新增此行（32 字节 AES-256 密钥）
CDNFLY_ENCRYPTION_KEY=base64:xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

- [ ] 已执行 `php artisan cdnfly:generate-key`
- [ ] 已执行 `php artisan cdnfly:rekey` 迁移现有凭证
- [ ] 已验证控制台页面 CDNfly 通讯正常

> **回滚方式**：`php artisan cdnfly:rekey --rollback` 可将凭证迁回 APP_KEY 加密。

### 4.2 API Key 自动轮换

已配置定时任务：每日凌晨 3:00 自动轮换超过 90 天未同步的 API Key。

```bash
# 手动触发（可选）
php artisan cdnfly:rotate-keys --days=90

# 仅预览
php artisan cdnfly:rotate-keys --dry-run

# 轮换指定用户
php artisan cdnfly:rotate-keys --user=5
```

- [ ] 已配置 `cron` 或 Supervisor 运行 `php artisan schedule:run`（轮换依赖调度器）

---

## 5. EPUSDT 支付服务

```env
EPUSDT_BASE_URL=https://pay.你的域名.com  # EPUSDT 服务公网地址
EPUSDT_API_TOKEN=（新生成的强 token）
EPUSDT_NOTIFY_URL=https://api.你的域名.com/api/payments/epusdt/notify
EPUSDT_REDIRECT_URL=https://portal.你的域名.com/payment/result
```

- [ ] EPUSDT 二进制已部署并运行（Docker 容器或独立服务）
- [ ] EPUSDT 管理面板已配置访问限制（IP 白名单或 Basic Auth）
- [ ] EPUSDT 管理 API 的 CORS 已通过 Nginx 限制为管理域名
- [ ] `EPUSDT_NOTIFY_URL` 已改为生产后端地址
- [ ] `EPUSDT_REDIRECT_URL` 已改为生产门户地址
- [ ] 已完成端到端支付测试：创建订单 → 支付 → 回调 → 订单状态更新

---

## 6. Docker 部署

### 6.1 首次部署

```bash
# 1. 复制并编辑生产环境配置
cp .env.example .env
# （按照上述各节编辑 .env）

# 2. 生成应用密钥
php artisan key:generate

# 3. 构建并启动所有服务
docker compose up -d --build

# 4. 运行数据库迁移
docker compose exec app php artisan migrate --force

# 5. 创建管理员账号（可选，如果需要 seed）
docker compose exec app php artisan db:seed

# 6. 验证服务状态
docker compose ps
docker compose exec app php artisan about
```

- [ ] 所有容器正常运行（`docker compose ps` 全部 Up）
- [ ] 数据库迁移已执行
- [ ] 管理员账号已创建
- [ ] `php artisan about` 输出正常

### 6.2 Nginx 反向代理（宿主机层）

Docker 内部 Nginx 监听 80（Laravel）和 8080（门户）。宿主机需要一个反向代理来：
- 处理 SSL 终止（Let's Encrypt）
- 将 `portal.你的域名.com` → Docker 8080
- 将 `api.你的域名.com` → Docker 80
- 限制 EPUSDT 管理 API 只允许内网或特定 IP 访问

- [ ] SSL 证书已申请并配置
- [ ] 门户域名正确代理到 Docker 8080 端口
- [ ] API 域名正确代理到 Docker 80 端口
- [ ] EPUSDT 管理端口（8001）未暴露到公网，或已设置 IP 白名单
- [ ] 已配置 HTTP → HTTPS 自动跳转

### 6.3 定时任务

`payments:reconcile` 命令每 5 分钟运行一次（已在 `routes/console.php` 中配置）。
Docker 的 `scheduler` 容器会自动执行 `php artisan schedule:run`。

- [ ] `scheduler` 容器正常运行
- [ ] 确认 `payments:reconcile` 按计划执行（查看日志）

### 6.4 队列

`queue` 容器运行 `php artisan queue:work`，处理异步任务。

- [ ] `queue` 容器正常运行
- [ ] 已验证邮件发送等异步任务正常处理

---

## 7. 品牌资产

- [ ] 控制台 Logo（`resources/js/components/AppLogoIcon.vue`）已替换为正式品牌 SVG
- [ ] 门户首页 hero 图（如有）已替换
- [ ] 邮件模板头图（如自定义了 Laravel Mail 模板）已替换
- [ ] Favicon（`public/favicon.ico` 和 `public/favicon.svg`）已替换

---

## 8. 安全加固

- [ ] 数据库只允许应用服务器连接（不暴露 3306 到公网）
- [ ] Redis 只允许内网连接（不暴露 6379 到公网）
- [ ] `.env` 文件权限为 600（仅 owner 可读写）
- [ ] `storage/` 和 `bootstrap/cache/` 目录权限正确（www-data 可写）
- [ ] 生产环境已运行 `php artisan config:cache` 和 `php artisan route:cache`
- [ ] 服务器防火墙只开放 80、443 端口
- [ ] SSH 仅允许密钥登录
- [ ] 管理端凭证迁移至 KMS（Vault / Docker Secrets / 云 KMS）

### 8.1 安全审计待办项

以下问题需在上线前处理：

- [ ] **CORS 收紧**：`config/cors.php` 开发环境允许任意 localhost 端口（`#^https?://localhost(?:\d+)?$#`），生产环境 `CORS_ALLOWED_ORIGINS` 必须只包含生产域名，删除所有 localhost/127.0.0.1 正则
- [ ] **v-html XSS**：`resources/js/components/TwoFactorSetupModal.vue:174` 使用 `v-html` 渲染 QR code SVG，需改用安全的 SVG 渲染方式（如 `<img :src="dataUri">` 或 DOMPurify 过滤）
- [ ] **Session Cookie Secure**：`.env` 中 `SESSION_SECURE_COOKIE=true`（HTTPS 部署后必须开启，已列在 2.3 节）
- [ ] **CSP 响应头**：添加 `Content-Security-Policy` 中间件或 Nginx 配置，至少设置 `script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; frame-ancestors 'none'`

### 8.2 KMS 凭证托管（待实施）

将以下高权限凭证从 `.env` 明文迁移至密钥管理服务，防止服务器被入侵后一次性泄露所有密钥：

| 凭证 | 优先级 | 说明 |
|------|--------|------|
| `CDNFLY_ADMIN_API_KEY` | 最高 | CDNfly 管理员权限，可操作所有用户 |
| `CDNFLY_ADMIN_API_SECRET` | 最高 | 同上 |
| `CDNFLY_ENCRYPTION_KEY` | 高 | 用户凭证加密根密钥 |
| `APP_KEY` | 高 | Session/Cookie 加密 |
| `DB_PASSWORD` | 中 | 数据库访问 |
| `EPUSDT_API_TOKEN` | 中 | 支付服务凭证 |

**可选方案（任选其一）：**

- **Docker Secrets**：零成本，适合单机 Docker Compose 部署
- **HashiCorp Vault**：适合多环境、需要审计日志和动态轮换
- **AWS Secrets Manager / 阿里云 KMS**：适合对应云平台部署

> 实施时需要提供：选用的 KMS 方案 + 访问地址/认证方式。

---

## 9. 监控与日志

- [ ] 日志输出已配置到持久化存储（Docker volume 或外部日志服务）
- [ ] 已配置异常报警（邮件或 Webhook，如 Sentry / Bark / Telegram Bot）
- [ ] MySQL 慢查询日志已开启
- [ ] 已配置服务健康检查（Docker healthcheck 或外部 uptime 监控）

---

## 10. 备份

- [ ] MySQL 已配置定时备份（mysqldump 或 Percona XtraBackup）
- [ ] 备份文件已配置异地存储（对象存储或其他机房）
- [ ] 已验证备份恢复流程

---

## 11. 前端构建

门户前端（`frontend/`）需要配置生产环境变量后重新构建。

```bash
# frontend/.env
VITE_API_BASE_URL=https://api.你的域名.com
VITE_DASHBOARD_BASE_URL=https://api.你的域名.com
VITE_RECAPTCHA_SITE_KEY=6Le...你的site key

# 构建
cd frontend && npm ci && npx vite build
```

- [ ] `frontend/.env` 已配置生产值
- [ ] 门户已构建并部署（`dist/` 目录）
- [ ] 已在浏览器中验证门户首页、登录、注册、找回密码页面正常

---

## 12. 端到端验证

上线后执行以下完整流程测试：

- [ ] 门户首页正常加载，套餐列表正常显示
- [ ] 注册新用户 → 收到验证邮件 → 点击链接验证成功
- [ ] 登录 → 跳转到控制台
- [ ] 找回密码 → reCAPTCHA 正常 → 收到重置邮件 → 重置成功
- [ ] 购买套餐 → EPUSDT 支付 → 回调成功 → CDNfly 自动开通
- [ ] CDNfly 站点管理 CRUD 正常
- [ ] 管理后台各页面正常加载和操作
- [ ] `payments:reconcile` 定时对账正常执行

---

## 13. 运维命令速查

### 定时任务（Scheduler 自动执行）

| 命令 | 频率 | 说明 |
|------|------|------|
| `payments:reconcile` | 每 5 分钟 | 对账待支付订单，重试已支付但未开通的订单 |
| `cdnfly:rotate-keys --days=90` | 每天 03:00 | 轮换超过 90 天的 CDNfly API Key |

> 定时任务依赖 `php artisan schedule:run` 被 cron 或 Docker scheduler 容器每分钟调用。

### CDNfly 凭证管理

```bash
# 生成独立加密密钥
php artisan cdnfly:generate-key

# 将凭证迁移到独立密钥
php artisan cdnfly:rekey --dry-run    # 预览
php artisan cdnfly:rekey              # 执行
php artisan cdnfly:rekey --rollback   # 回滚

# 手动轮换 API Key
php artisan cdnfly:rotate-keys --dry-run      # 预览
php artisan cdnfly:rotate-keys --days=90      # 轮换 90 天以上
php artisan cdnfly:rotate-keys --user=5       # 轮换指定用户
```

### 支付对账

```bash
# 手动触发对账（通常不需要，scheduler 自动执行）
php artisan payments:reconcile
```

### 安全验证

```bash
# 运行安全相关测试
php artisan test --filter=CdnflySecurityTest

# 验证 CDNfly 通讯是否正常（看有无 502 错误日志）
tail -f storage/logs/laravel.log | grep -i cdnfly
```

### 前端构建与检查

```bash
# 类型检查（不输出文件）
npx vue-tsc --noEmit

# 开发服务器
npx vite

# 生产构建
npx vite build

# 检查某个组件是否有类型错误
npx vue-tsc --noEmit 2>&1 | grep "UserSites"
```

### CDNfly 代理白名单维护

当需要暴露新的 CDNfly API 给前端时：

1. 编辑 `app/Http/Controllers/Api/CdnProxyController.php` 的 `ALLOWED_PROXY_PREFIXES`
2. 在 `resources/js/lib/cdnUserApi.ts` 添加对应的 API 函数
3. 前端通过 `proxyGet()` / `proxyRequest()` 调用

```php
// CdnProxyController.php — 白名单格式：
private const ALLOWED_PROXY_PREFIXES = [
    '/v1/路径前缀' => ['允许的HTTP方法'],
    // 例如：
    '/v1/sites' => ['GET', 'POST', 'PUT', 'PATCH', 'DELETE'],
    '/v1/user-packages' => ['GET'],  // 只读，写入需通过订单流程
];
```

### 用户排障

```bash
# 查看某用户的 CDNfly 同步状态
php artisan tinker --execute="
  \$u = App\Models\User::find(用户ID);
  echo 'cdnfly_user_id: '.\$u->cdnfly_user_id.PHP_EOL;
  echo 'has_key: '.(\$u->hasCdnflyApiKey() ? 'yes' : 'no').PHP_EOL;
  echo 'synced_at: '.\$u->cdnfly_synced_at.PHP_EOL;
"

# 手动为某用户重新同步 API Key
php artisan cdnfly:rotate-keys --user=用户ID
```
